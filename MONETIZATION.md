# ポケふたコレクト — 広告導入計画

**現時点でこの文書はコード変更を一切伴わない。** 広告SDKは未導入。既存の
収集・カメラ・位置情報・メダル判定機能への影響ゼロを確認できるまで、
実装には着手しない。ROADMAP.md / TASKS.md と同じ位置づけの文書として、
リポジトリ直下に置く。

---

## 0. 結論（先に）

1. **このアプリは「Web/PWA専用、ネイティブストア配信をしない」という既存決定
   （ROADMAP.md §6）を既にしている。** これは広告SDKの選択肢を事実上
   確定させる制約になる：ネイティブの Google Mobile Ads SDK（AdMob）は
   ストア配信前提のビルドパイプラインを要求するため選べない。**現実的な
   選択肢は Web ベースの広告（Google AdSense 等）に限られる。** ネイティブ
   （Expo の実機ビルド）には当面広告を出さない。これは新しい制約ではなく、
   既存の「ストア配信をしない」判断をそのまま延長しただけ。
2. **helmet の CSP が既に厳格**（`script-src 'self'` + 固定ハッシュ1つのみ）。
   このリポジトリは CSP がらみで**過去に2回**実害を出している
   （Service Worker登録・地図のインラインスクリプト、いずれも CLAUDE.md
   に事故として記録済み）。広告ネットワークの導入は**3度目の同じ罠**に
   なる可能性が最も高い変更なので、実装時は必ず本番で `curl`／実機確認を
   行うこと。
3. **既存の判断原則（ROADMAP.md §3）がそのまま広告の設計原則になる。**
   特に「屋外・片手・電波が弱い場所」「個人開発・保守コスト最小」は、
   広告SDKの選定・失敗時の挙動・頻度制御の設計に直接効く。新しい原則を
   作る必要はなく、既存の原則を広告に適用するだけでよい。
4. **本命候補は Google AdSense。** 理由は §9 参照。他のネットワークへの
   差し替えを見据えたコンポーネント設計（§6）にしておけば、後から
   変更しても画面側のコードはほぼ触らずに済む。

---

## 1. このアプリの現状（調査結果）

### 1.1 画面構成

| 画面 | ファイル | 役割 |
|---|---|---|
| 一覧（ホーム） | `app/(tabs)/index.tsx` | 進捗ヒーロー、市区町村の「もう少しで達成」、「次に集めよう」横スクロール、都道府県別 `SectionList` |
| 地図 | `app/(tabs)/map.web.tsx` / `map.tsx` | Leaflet（Web: iframe srcDoc）/ WebView（Native）、クイック記録フロー |
| コレクション | `app/(tabs)/collection.tsx` | 統計カード、`FlatList` グリッド（`PokeLidCard`） |
| 設定 | `app/(tabs)/settings.tsx` | ログアウト、未確認メール案内、メダル凡例、法的リンク、アカウント削除 |
| ポケふた詳細 | `app/poke-lids/[id].tsx` | 情報カード、撮影/プレビュー/アップロード/`CelebrationModal` |
| 都道府県詳細 | `app/prefectures/[id].tsx` | 市区町村グループ化グリッド |
| login/register/forgot-password/reset-password/verify-email/auth/google/callback | 認証フロー一式 |

**「最重要体験」の実装箇所**（広告を挟んではいけない範囲、ユーザー指示の
とおり）は具体的に以下の状態変数がアクティブな間：

- `pendingPhoto !== null`（`PhotoPreviewModal` 表示中） — `poke-lids/[id].tsx`, `useQuickRecord.ts`
- `uploading === true`（アップロード中、進捗バー表示中）
- `celebration !== null`（`CelebrationModal` 表示中）
- **見落としやすい追加項目**：`auth.tsx` の `syncProgress !== null` /
  `syncCelebration !== null`（7-9フェーズ2、ゲスト記録同期時の
  `UploadProgressBanner`／`SyncCompleteModal`）。これも「撮影→判定→
  メダル獲得」と同種の報酬体験であり、同じ扱いにすべき
- `Onboarding.tsx` の初回オーバーレイ、`InstallPrompt.tsx` のインストール
  案内が表示されている間（別のオーバーレイと広告を同時に出さない）

### 1.2 PWA / Web 構成

- `apps/mobile/app.json` の `web.output: "static"`（Expo Router 静的書き出し）
- `apps/api/src/index.ts` が `apps/api/public`（=書き出したWebビルド）を
  配信、`helmet` で CSP を設定（§1.3）
- `apps/mobile/public/sw.js` … Service Worker。**広告ネットワークへの
  リクエストは自動的にキャッシュ対象外**（`fetch` ハンドラが
  `if (url.origin !== self.location.origin) return;` で他オリジンを
  素通りさせているため）。ここは追加対応不要と確認済み
- `apps/mobile/public/manifest.json` … PWA マニフェスト
- ネイティブ（Expo の実機ビルド）はストア未配信（ROADMAP.md §6）。
  今回の計画は **Web版のみを対象にする**

### 1.3 CSP の現状（`apps/api/src/index.ts`）

```ts
contentSecurityPolicy: {
  directives: {
    ...helmet.contentSecurityPolicy.getDefaultDirectives(),
    'img-src': ["'self'", 'data:', 'blob:', 'https://*.tile.openstreetmap.org'],
    'script-src': ["'self'", "'sha256-...'"], // Expo Routerのブートストラップ用ハッシュ1つのみ
  },
},
```

広告ネットワークのスクリプトはほぼ例外なく、①外部ドメインからの
`<script>` 読み込み ②広告自身が動的に生成する追加スクリプト・iframe
③トラッキング用の `connect-src`／`img-src` 送信、を必要とする。
**現在の CSP はこれらを全てブロックする設定になっている。** 実装時に
`script-src`／`frame-src`／`connect-src`／`img-src` を広告ネットワークの
ドメインに合わせて緩めることが必須になる。

> 補足：本番の実機確認で `static.cloudflareinsights.com` への beacon が
> CSP でブロックされているのを確認済み（Cloudflare が edge 側で自動注入する
> Web Analytics ビーコンで、このリポジトリのコードには存在しない）。実害は
> ないが、「このサイトは既に一つ、CSPでブロックされている第三者スクリプトを
> 持っている」という既成事実として認識しておくとよい。

### 1.4 グリッド実装の既知の罠（コレクション画面）

`useGridData.ts` の `padToColumns` は、`FlatList` の `numColumns` が
最終行を stretch させる問題（CLAUDE.md に事故として記録済み）を、
**データ件数が列数の倍数になるよう `null` プレースホルダーで埋める**
ことで解決している。

**広告カードをグリッドの1アイテムとして挿入するのは高リスク。**
`padToColumns` の列数倍数の前提と `gridKeyExtractor` の null 判定に
干渉し、同種のレイアウト崩れを再発させる可能性が高い。広告は
グリッドの**外**（`ListHeaderComponent` 内、統計カードとグリッドの間）
に置くこと。

---

## 2. 画面別の評価

ユーザー提示の表を、実際のコンポーネント名・具体的な配置根拠で肉付けした。

| 画面 | 広告 | 推奨形式 | 配置場所（具体） | 理由・注意 |
|---|---|---|---|---|
| ホーム (`(tabs)/index.tsx`) | ○ | ネイティブ風バナー1枠 | `ListHeaderComponent` 内、「次に集めよう」の横スクロールと `SectionList` の間 | `SectionList` の行として周期的に差し込むのは複雑・高リスク（下記§2.1）。まず単発の1枠から |
| コレクション (`(tabs)/collection.tsx`) | ○ | コンテンツ間 | `ListHeaderComponent` 内、統計カードとグリッドの間（**グリッドのアイテムとしては絶対に入れない**、§1.4） | カードの視認性を守りつつ自然に配置できる |
| 地図 (`(tabs)/map.web.tsx`, `map.tsx`) | △→当面×推奨 | 出すとしても小型バナー | 出すなら `filterRow` の下・地図(iframe/WebView)の外側のみ | `UploadProgressBanner`（下部固定）、`PhotoPreviewModal`、`CelebrationModal` が既に画面下部〜全体を使う。地図はこのアプリの中核機能（ROADMAP.md §1）でもあり、**Phase 1/2では出さない**ことを推奨 |
| ポケふた詳細 (`poke-lids/[id].tsx`) | △ | 下部・控えめ | `ScrollView` 最後尾、「写真を撮って記録する」等のCTAより**下** | CTAより上・CTAの間には絶対に置かない。撮影/プレビュー/判定中は非表示 |
| 都道府県詳細 (`prefectures/[id].tsx`) | ○ | コンテンツ間 | 市区町村グリッドの合間（コレクション画面と同じ注意） | 未調査の追加候補。実装時に構造を再確認すること |
| 設定 (`(tabs)/settings.tsx`) | ○ | 小型 | メダル凡例カードと法的リンクカードの間（**「アカウントを削除する」ボタンの近くには置かない**） | 危険操作の近くに広告を置くと誤タップ・誤解のリスク |
| カメラ／写真選択・プレビュー | × | なし | — | 撮影体験を阻害 |
| メダル判定・アップロード中 | × | なし | — | `uploading`・`pendingPhoto` 中は`AdSlot`ごと非表示 |
| `CelebrationModal` / `SyncCompleteModal` | × | なし | — | 報酬体験。ゲスト同期完了演出（7-9フェーズ2）も同格に扱う |
| ログイン/登録/パスワード関連画面 | × | なし | — | コンバージョンの障害物にしかならない。対象外でよい |

### 2.1 なぜホームの「周期的な差し込み」を今回は推奨しないか

`SectionList` に一定間隔で広告行を差し込む案（ユーザー案の
「地方セクションの合間」等）は技術的に可能だが、`groupByRegion` の
セクション構造・`renderSectionHeader`・`keyExtractor` に手を入れる必要が
あり、変更範囲がホーム画面のコアロジックに直接及ぶ。ROADMAP.md §3-4
（個人開発・保守コスト）に照らすと、**最初から複雑な統合を選ぶ理由がない**。
まず `ListHeaderComponent` 内の単発1枠で効果を見て、必要になったら
周期差し込みを検討する（§12 のフェーズ分けを参照）。

---

## 3. コンポーネント設計（提案・未実装）

要件10「広告サービスを画面に直接埋め込まない」を満たすため、3層に分離する。

```
apps/mobile/src/ads/
  AdSlot.tsx            画面が唯一importするコンポーネント。表示可否の判定と
                         状態（loading/loaded/failed/blocked）だけを持つ
  useAdEligibility.ts   AdSlot専用フック。packages/sharedの純粋関数を呼び、
                         AsyncStorage/sessionStorageの頻度カウンタを読み書きする
  providers/
    adsenseProvider.ts   AdSenseの<script>読み込み・広告ユニットの初期化。
                         広告ネットワークを変更するときはこのファイルだけ差し替える
    noopProvider.ts       開発環境・広告未設定時のダミー実装（何も描画しない）
```

```
packages/shared/src/ads.ts（新規、既存のdetermineMedal/decideGoogleAccountLink
                            と同じ「純粋関数＋vitestテスト」パターン）

  export type AdSlotId = 'home-feed' | 'collection-feed' | 'settings' | 'detail-footer' | 'interstitial';

  export interface AdEligibilityContext {
    slotId: AdSlotId;
    // 現在アクティブな最重要体験があるか（撮影/判定/演出/同期演出）。
    // true の間、どのスロットも広告を出さない。
    inCriticalFlow: boolean;
    // 将来の有料プラン拡張の受け口。現状は常に false。
    isAdFree: boolean;
    sessionAdShownCount: number;
    lastAdShownAtMs: number | null;
    lastCelebrationAtMs: number | null;
    nowMs: number;
  }

  export function decideAdEligibility(ctx: AdEligibilityContext): boolean { ... }
```

**なぜ `packages/shared` に置くか**：`decideGoogleAccountLink` /
`determinePhotoMedal` と同じ理由。UIから独立した純粋関数にしておくことで、
「取得直後は抑制」「最低表示間隔」「セッション上限」といった条件をvitestで
検証できる（このリポジトリの既存流儀：DBやネットワークが絡まない判定
ロジックは必ずshared側でテストする）。`AdSlot` コンポーネント自体は
「表示するかどうか」を自分で判断しない — 判断は全て `decideAdEligibility`
に委ね、コンポーネントは結果に従って描画するだけにする。

### 3.1 状態管理（要件11: 読込中/成功/失敗/非対応/オフライン）

`AdSlot` の内部状態は4つに限定する：

- `hidden`（初期値・判定NGの間・広告主が空を返した間）— **高さ0、DOMに何も残さない**
- `loading`（判定OK・読み込み中）— 高さ0のまま（レイアウトシフトを起こさない）
- `loaded`（表示成功）— 実際の広告の高さぶんだけ展開
- `failed`（タイムアウト or エラー）— `hidden` と同じ扱いに戻す。**再試行しない**
  （電波が弱い環境で何度もリトライすると通信量を無駄に消費する。ROADMAP.md
  §3-2「通信を減らす。失敗を前提にする」）

固定の高さを先に確保しない設計にすることで、「巨大な空白が残る」問題を
構造的に避ける。オフライン時は `navigator.onLine`（Webのみ）を一度だけ
確認し、オフラインなら `loading` にすら入らず即 `hidden` にする（無駄な
リクエストを発火させない）。

### 3.2 将来の有料プラン拡張

`AdEligibilityContext.isAdFree` を今から用意しておき、`decideAdEligibility`
の先頭で `if (ctx.isAdFree) return false;` とするだけで、実装コストゼロで
将来の分岐点になる。現時点で課金機能を作る必要はない（要件12のとおり）。

---

## 4. インタースティシャル（全画面広告）の設計方針

**Phase 3 まで実装しない**（§12）。設計だけ先に決めておく。

- トリガー候補は「記録イベントに直結させない」ことを絶対条件にする
  （「取得した直後に広告」を避けるため、記録完了イベントそのものを
  トリガーにしない）
- `lastCelebrationAtMs` からの経過時間を `decideAdEligibility` でチェックし、
  一定時間（初期案: 5分）以内はインタースティシャル自体をブロックする
- セッション概念（アプリを開いてから閉じるまで）は現状このリポジトリに
  存在しない（`AppState` 監視なし）。導入する場合はこれ自体が新規実装
  になる点に注意
- 頻度キャップの初期案：
  - 初回起動から数セッションは完全抑制（`InstallPrompt` の「2週間再表示
    しない」と同じ、AsyncStorageに1つタイムスタンプを持つだけの軽量な
    実装で足りる）
  - セッションあたり最大1〜2回
  - 最低表示間隔を分単位で設定

---

## 5. 頻度制御・状態の永続化

既存の `installPrompt.ts`（7-10、「一度閉じたら2週間再表示しない」）と
`onboarding.ts` が、**このリポジトリで既に確立している「うるさくしない
オーバーレイ」パターン**。広告の頻度制御もこれをそのまま踏襲する
（AsyncStorage/localStorageにタイムスタンプ・カウンタを1〜2個持つだけ）。
新しい仕組みを発明しない。

---

## 6. 広告サービスを変更しやすくする設計（要件10）

- 画面側が触るのは `<AdSlot slotId="home-feed" />` だけ
- 広告ネットワーク固有のコード（スクリプトタグ、広告ユニットID、
  レンダリングAPI）は `providers/` 配下の1ファイルにしか存在しない
- 広告を全面OFFにする（規約違反の指摘があった場合、広告主都合の停止など）
  ときは、`providers/noopProvider.ts` に差し替えるだけで全画面から広告が
  消える設計にする
- 広告ネットワークのスクリプトは `apps/mobile/public/` 配下の外部ファイル
  経由で読み込む（`sw-register.js` と同じパターン）。CSPの `script-src`
  に対象ドメインを追加する必要はあるが、インラインスクリプトを増やさない
  ことで CSP 事故のリスクを最小化する

---

## 7. パフォーマンスへの影響（要件15）

- 広告SDKのスクリプトは Metro のバンドルに含めず、`<script async>` で
  外部読み込みする（`sw-register.js` と同じ既存パターンの再利用）
- 初期表示のクリティカルパスから広告読み込みを外す（画面が描画し終わって
  から広告を読み込み始める、いわゆる遅延読み込み）
- Service Worker のキャッシュ戦略（`sw.js`）は広告リクエストを素通り
  させる構成に既になっている（§1.2で確認済み、追加対応不要）
- モバイル回線への配慮（ROADMAP.md §3-2）から、広告表示の失敗時に
  リトライしない設計（§3.1）自体が通信量削減にも効く

---

## 8. プライバシー・同意管理（要件16）

現状の確認：

- Cookie／トラッキングは現状アプリ側に一切存在しない
  （認証は Bearer トークン、位置情報・写真は本人操作のみで送信）
- `/privacy`・`/terms` は既に存在するが、過去の実装記録に「草案」表示
  ありと記録されている（本番投入前に内容確定が必要、というのは広告導入
  以前からの既存タスク）

広告導入で**新たに**必要になるもの：

1. **プライバシーポリシーの更新**：Cookie／広告識別子／パーソナライズ
   広告の利用有無を明記
2. **CMP（同意管理）の要否判断**：EU/UK ユーザーにパーソナライズ広告を
   出す場合、Google の Consent Mode / IAB TCF 相当の同意取得が実質必須。
   **Phase 1 では非パーソナライズ広告（contextual ads）に限定し、CMP
   導入は見送る**ことを推奨。実装コストと法的リスクの両方を大きく
   下げられる（ROADMAP.md §3-4 の保守コスト最小方針とも合致）
3. **CCPA（カリフォルニア）**：同じく非パーソナライズ広告に限定すれば
   "Do Not Sell" 対応の優先度は下がる
4. **広告のUX影響を測るための新規クライアント計測は追加しない**（§10で
   判断済み）。広告ネットワーク自身が設置するCookie／広告識別子は上記
   1〜3の対応が必要だが、それとは別に「継続率が落ちていないか」を
   測るための**自前のトラッキングコードは足さない**。これにより、この
   節で新たに増えるプライバシー面での検討事項は広告ネットワーク側の
   ものだけに限定される（計測基盤自体が新しいプライバシーサーフェスに
   ならない）
5. **COPPA（児童プライバシー）— このアプリ特有の見落としやすいリスク**：
   「ポケモン」を題材にしたコンテンツは、Google 側の審査で
   **「主に児童向け」と誤って分類されるリスクがある**。誤分類されると
   パーソナライズ広告が一切使えなくなるだけでなく、規約上の対応義務が
   発生する。実際のユーザー層（マンホールを巡って旅行する成人の
   ホビーユーザー）と、题材（ポケモン）のギャップを踏まえ、**AdSense
   申請時に児童向け表示の対象外であることを明示的に宣言する**必要がある。
   これは技術実装より先に確認すべき事項

---

## 9. 広告ネットワークの候補（要件13関連）

**本命：Google AdSense。**

理由：
- Web専用というこのアプリの制約（§0-1）に最初から合致し、ネイティブSDK
  統合の複雑さが一切発生しない
- 単一の `<script>` タグで導入でき、個人開発の保守コストに見合う
  （メディエーションSDK・複数ネットワーク管理は不要）
- コンテンツ連動型広告（contextual ads）が強く、要件13（旅行用品・
  カメラ用品・交通・宿泊など）で挙げられたジャンルとの親和性は、
  パーソナライズなしでも記事内容ベースである程度自然に働く
- プライバシーポリシー・利用規約のページが既に存在する（審査要件を
  既に満たしやすい状態）
- 481件の詳細ページ＋都道府県ページという十分なオリジナルコンテンツ量が
  既にある（審査通過の実質条件）

**注意点**：AdSense のアカウント作成・サイト審査は人間（ユーザー）が
行う必要があり、Claude Code からは実行できない。実装を始める前に
アカウント取得・審査通過が前提になる（Google SSO の `GOOGLE_CLIENT_ID`／
`GOOGLE_CLIENT_SECRET` を人間が Google Cloud Console で取得したのと
同じ立て付け）。

---

## 10. 計測設計（Phase 0）

> **2026-08-13追記**：この節は当初のドラフトになかった。「広告導入前に
> UX/計測設計をレビューしてほしい」という依頼を受けてレビューした結果、
> **「UXを壊さない」という本文書の目標を検証する手段が一つも設計されて
> いない**ことが最大のギャップと判明したため追加した。旧 §10〜§12 は
> §11〜§14 に繰り下げている。

### 10.1 方針：新規クライアント計測は追加しない

検討の結果、**新しいトラッキングコード（分析SDK・自前のイベント送信）は
一切追加せず、既に機能上の理由でDBに記録されているデータの分析だけで
Phase 1のUX影響を判断する**方針とした。

理由：
- このアプリは現在 Cookie／行動トラッキングが一切ない状態（§8）。これを
  崩さずに広告のUX影響を検証できる
- 広告ネットワーク自身の同意管理（§8）とは別に、計測基盤自体の同意管理を
  検討する必要が生じない
- ROADMAP.md §3-4（個人開発・保守コスト最小）に最も合致する

裏を返すと、**取れる指標の粒度には限界がある**（画面単位のUI操作イベント
は取れない、`CelebrationModal`の「次へ」タップ率のような細かい指標は
測れない）。これは意図的なトレードオフであり、後で「もっと細かく見たい」
となった場合は改めて判断し直すこと（そのときに初めて、新規計測を足す
かどうかの検討をする）。

### 10.2 既存DBから取れる代理指標

新しいテーブル・カラムの追加は不要。既存のPrismaスキーマの列だけで
算出できる。

| 指標 | 算出元 | 何の代理か |
|---|---|---|
| 新規登録数 | `User.createdAt` の日次件数 | 流入 |
| 記録頻度 | `Collection.createdAt` の日次件数（ユーザー当たり） | 利用の活発さ |
| **初回→2回目記録までの日数** | 同一 `userId` の `Collection.createdAt` を昇順に並べ、1件目と2件目の差分 | **③目的化の成否そのもの**（ROADMAP.md §2）。広告のUX影響で最も見たい指標 |
| セッション活動の頻度 | `RefreshToken.createdAt` の頻度 | リフレッシュはローテーション方式（5-2）で毎回新しい行を作るため、アクセストークン更新＝アプリを開いて使った、の代理になる |
| メダル体験の質 | `Photo.medal` が `GOLD` の割合の推移 | 直接は広告と無関係だが、「広告に気を取られて雑な撮影が増えた」といった間接的な質の低下があれば動く可能性がある指標 |

これらは全て `Collection`/`User`/`RefreshToken`/`Photo` の既存カラムのみで
算出可能で、**マイグレーション不要・アプリ側のコード変更も不要**。

### 10.3 集計方法

このアプリに管理画面・ダッシュボードは存在せず、今回もダッシュボード化は
しない（過剰投資）。`apps/api/scripts/`（`dump-poke-lids.ts` 等の既存の
単発スクリプトと同じパターン）に、期間を指定して上記指標を出力する
軽量スクリプトを1本追加する程度に留める。

### 10.4 Phase 0 の実施手順

1. Phase 1（広告表示開始）の**前に**、直近4週間分の§10.2指標を算出し、
   この文書か別ファイルに記録として残す（ベースライン）
2. Phase 1開始後、同じ期間・同じ指標を再算出する
3. §10.6の基準に照らして継続可否を判断する

**この手順自体がPhase 1着手の前提条件であり、Phase 1のタスクリスト
（§12）にも明記する。**

### 10.5 段階的ロールアウト（%ゲート）

feature flag基盤を新設せずに実現できる、最小コストの案：

`decideAdEligibility`（§3）の `AdEligibilityContext` に
`rolloutBucket: number`（0〜99の整数、ユーザーID/ゲストセッションIDを
ハッシュ化して算出）を1項目追加し、`ROLLOUT_PERCENTAGE`のような環境変数
としきい値比較するだけ。新しいインフラは不要で、既存の「純粋関数＋
vitestテスト」パターンに1条件足すだけで済む。Phase 1は10〜20%程度から
始め、§10.4の指標に異常がないことを確認してから引き上げることを推奨する。

### 10.6 ロールバック基準（初期案）

現時点ではベースラインの実データがないため、**具体的な閾値は決め打ち
できない**。ただし「基準を決めずに走り出さない」ことだけは今の時点で
確定させる。

初期案（Phase 0実施後、実データを見て具体的な数値に置き換えること）：

> Phase 1のロールアウト対象ユーザーにおいて、初回→2回目記録までの
> 到達率が、ベースライン（広告導入前）と比べて統計的に有意な悪化を
> 示した場合、ロールアウト比率を0に戻し、原因（配置・頻度・特定画面）
> を切り分けてから再開する。

### 10.7 キルスイッチの再設計（再デプロイ不要にする）

§6で触れた `noopProvider.ts` への切り替えはコード変更＋再デプロイが要る。
このリポジトリの実際のデプロイ（`docker compose build --no-cache` の
実績）はビルドだけで5〜10分かかるため、**悪化を検知してから止めるまで
の時間**として長すぎる。

広告の有効/無効・ロールアウト比率（§10.5）は、**クライアントの静的
ビルドに焼き込まず、サーバー側の軽量エンドポイントから都度読む**構成に
する（`GOOGLE_CLIENT_ID` のようにビルド時に埋め込む必要はない — 広告
ネットワークのクライアントIDと違い、これは公開しても問題のない値では
なく、単なるON/OFFなので実行時に読めばよい）。`robots.txt`／
`sitemap.xml` を都度動的生成している既存パターンや、TASKS.md 7-7で
提案されている軽量バージョンチェックエンドポイント（`GET
/api/poke-lids/version` 案）と同じ考え方でよい。これなら `.env` を
書き換えて `docker compose up -d`（フルビルドではなくコンテナ再作成の
み）するだけで、数秒〜数十秒でロールアウト比率を0に戻せる。

---

## 11. UX設計の残課題

計測とは別に、レビューで見つかった**UX設計そのものの抜け**。実装コストは
小さいが、Phase 1のチェックリストに加えるべき項目。

- **極小デバイスでの崩れ未検証**：ROADMAP.mdの「完成の定義」にある
  「320px幅・フォント最大設定でどの画面も崩れない」に対して、広告
  バナー追加後の再確認が計画にない。広告の高さが確定した瞬間
  （§3.1 `loaded` 状態）にCTAが画面外へ押し出されるケースを、実装時に
  必ず確認すること
- **reduce motion 配慮の欠落**：`haptics.ts` が既に「reduce motion」設定
  を尊重している。広告クリエイティブ（アニメーションバナー等）がこの
  既存の配慮と矛盾しないか、少なくとも自動再生アニメーション広告は
  避けるという方針を明記しておくべき
- **頻度キャップの数値が根拠のない仮置き**（§4「初期案: 5分」
  「1〜2回」）。§10.5のロールアウト機構と組み合わせ、実データを見ながら
  調整する前提であることを明記する

---

## 12. フェーズ分け（実装ロードマップ）

ROADMAP.md の M0〜M4 と同じ粒度で整理する。**広告は既存の M0〜M4 に
割り込まない。** 特にM0（事故を止める）とM1（公開に耐える状態）を
広告より優先する。

### Phase 0（新規・§10）

- §10.2の指標をベースラインとして算出・記録する
- `apps/api/scripts/` に集計スクリプトを1本追加する
- §10.5のロールアウト機構、§10.7のキルスイッチを`AdSlot`基盤と同時に実装する
- **この手順が完了するまでPhase 1（広告の実表示）には進まない**

### Phase 1（影響が小さい場所のみ）

- `AdSlot` / `decideAdEligibility` の基盤実装（`packages/shared` にテスト付きで追加）
- ホーム画面：`ListHeaderComponent` 内、単発1枠
- コレクション画面：`ListHeaderComponent` 内、統計カードとグリッドの間、単発1枠
- 設定画面：メダル凡例と法的リンクの間、単発1枠
- 非パーソナライズ広告のみ（CMP不要な構成）
- CSP変更・本番での実機確認（curlベースのCSPブロック確認を含む）

### Phase 2（利用状況を見ながら）

- 都道府県詳細画面への導入
- ポケふた詳細画面（下部・CTAより後ろ）
- ホームの「周期的な差し込み」を検討するならここで
- 地図画面は依然として慎重（§2の理由により、このフェーズでも見送り候補）

### Phase 3（必要になったら）

- インタースティシャル（§4の設計を実装）
- 高頻度利用ユーザー向けの最適化
- 広告非表示の有料プラン（`isAdFree` フラグは Phase 1 から用意済みなので
  実装コストは小さい）
- 地図画面への導入を再検討（それでも「地図を覆わない」制約は維持）

---

## 13. 意図的にやらないこと（ROADMAP.md §6 と同じ形式）

### ネイティブ広告SDK（AdMob等）の導入

ROADMAP.md 既存決定「ネイティブアプリのストア配信をしない」と矛盾する。
ストア配信を将来やると決めたときに合わせて再検討する。

### パーソナライズ広告・CMP導入

Phase 1では非パーソナライズに限定する（§8）。海外ユーザーへの
パーソナライズ広告配信は、実際のトラフィックが要求するようになってから
CMP導入とセットで検討する。

### 広告の周期的な自動差し込み（SectionList/グリッドへの統合）

`padToColumns` 等、既存のグリッド実装の前提を壊すリスクが高い
（§1.4, §2.1）。Phase 1では単発スロットに留める。

### 地図画面への広告

このアプリの中核機能（ROADMAP.md §1）であり、`UploadProgressBanner`
等の既存オーバーレイとの競合リスクも高い。Phase 3まで見送る。

### 新規のクライアント側行動計測（分析SDK・自前イベント送信）

§10.1で判断済み。広告のUX影響は既存DBデータの分析（§10.2）だけで見る。
`CelebrationModal`のタップ率のような画面内操作の細かい計測は、この方針を
取る限り得られない。取りたくなった場合は、その時点で改めて
「新しいプライバシーサーフェスを追加してでも取る価値があるか」を
判断し直すこと（自動では追加しない）。

---

## 14. 未決定・人間の判断が必要な事項

1. **AdSense アカウントの作成・サイト審査**（Claude Code では実行不可）
2. **COPPA/児童向け分類の申告内容**（§8-5）— 実際のユーザー層をどう
   Google に説明するか
3. **§10.6のロールバック基準の具体的な数値** — Phase 0でベースラインを
   取るまでは決め打ちできない。実データを見て確定させること
4. 広告収益と「偶然を目的に変える」というアプリの核（ROADMAP.md §1）の
   両立は、§10の計測（既存DBデータの分析）とロールバック基準（§10.6）で
   判断する。**「使ってみて感覚で判断する」ではなく、数値で判断する**
   という点が、当初のドラフトからの一番の変更点
