# ポケふたコレクト — 残タスク指示書（Claude Code 用）

このドキュメントは、コードレビューで洗い出された未対応項目をまとめたものです。
**タスクは1つずつ渡してください。** まとめて渡すと変更範囲が広がりすぎ、レビューもロールバックも困難になります。

---

## 0. 前提

作業ルール・規約・このリポジトリ特有の落とし穴は **`CLAUDE.md`** に記載してある。
Claude Code は起動時に自動で読み込むので、指示に貼り直す必要はない。

タスクを渡すときは、このドキュメントの該当セクションだけを貼ればよい。

### 最初に1回だけ実行

```
`npx prettier --write .` を実行し、フォーマット差分だけをコミットしてください。
現在 CI の prettier チェックが失敗する状態です。他の変更は含めないこと。
```

---

### プロジェクト名の変更

```
プロジェクトの表示名を「ポケふた収集」から「ポケふたコレクト」に変更してください。

対象は20箇所（`grep -rn "ポケふた収集" --include="*.tsx" --include="*.ts" --include="*.json" --include="*.md" . | grep -v node_modules` で確認できる）。

注意点:
1. `apps/mobile/app/(tabs)/index.tsx` のヒーローには「ポケふたコレクション」という
   別表現がある。これも「ポケふたコレクト」に統一するか、意図的な使い分けとして
   残すかを判断すること。
2. `apps/mobile/app/(tabs)/_layout.tsx` の「ポケふた収集進捗」は
   「ポケふたコレクト」ではなく「収集進捗」など、画面名として自然な形にすること。
   機械的な置換では不自然になる。
3. `terms.tsx` と `privacy.tsx` の本文中の名称も忘れずに。
   法的文書なのでサービス名の表記揺れがあってはならない。
4. `app.json` の `name` と `public/manifest.json` の `name` / `short_name` を変更する。
   **`slug`（pokelids-collect）は変更しないこと。** ビルドやデプロイの識別子であり、
   ドメイン `pokelids-collect.jp` とも対応している。
5. OGP のタイトル（`+html.tsx` の meta タグ）も変更対象。
6. 変更後、`npx prettier --check .` と `npm run test` を通すこと。
```

---

# フェーズ1: 事故防止（最優先）

放置すると実害が出るものです。ここから着手してください。

## 1-1. ETL に件数の安全装置を入れる

**背景**
`apps/api/etl/scrape.ts` は `local.pokemon.jp` の HTML をセレクタ（`a.manhole-detail[href]` 等）でパースしている。公式サイトがリニューアルしてセレクタが変わると、各都道府県のパースが 0 件になっても `console.warn` して `continue` するだけで、**正常終了してしまう**。この状態でスナップショット JSON が上書きされ、後続タスク（1-2 の論理削除）を入れた後だと全ポケふたが一斉に「撤去済み」になる。

**変更内容**
- DB 上映の既存件数と、今回スクレイプできた件数を比較する
- 前回比 90% を下回ったら `throw` して DB 更新とスナップショット書き込みの**両方**を行わない
- 意図的に強行するための `--force` フラグを用意する
- 併せて、各都道府県で 0 件だった場合は `console.warn` ではなく警告として集計し、最後にまとめて表示する

**受け入れ基準**
- 件数が閾値を下回った場合、DB は一切変更されず、スナップショット JSON も書かれない
- エラーメッセージに「前回 N 件 → 今回 M 件」が含まれ、原因が推測できる
- `--force` 付きなら従来どおり実行できる

---

## 1-2. アップロードの上限を設ける

**背景**
`POST /api/collections` にレート制限がなく、1つの収集記録に登録できる写真の枚数上限もユーザーあたりの総容量上限もない。ログイン済みユーザーは 25MB の画像を無制限にアップロードできる。`docker-compose.yml` を見ると Postgres のデータディレクトリと写真ディレクトリが同一ホスト上にあるため、**ディスクが埋まると Postgres が書き込み不能になりサービス全体が停止する**。

**変更内容**
`apps/api/src/routes/collections.ts` に3層の制限を入れる。

1. `express-rate-limit` によるアップロード回数制限（既存の `credentialRateLimit` と同じ流儀で。目安: 60回/時）
2. 1収集記録あたりの写真枚数上限（目安: 5枚）
3. ユーザーあたりの総容量上限（`Photo.fileSizeBytes` の `aggregate` で算出。目安: 500MB）

上限超過時は 400 / 413 と**日本語のエラーメッセージ**を返し、クライアント側でそのメッセージが表示されること。

**受け入れ基準**
- 上限値は環境変数で調整可能（デフォルト値をコード内に持つ）
- 容量チェックは1クエリで済ませる（N+1 を作らない）
- クライアント側で上限超過のメッセージがユーザーに見える（1-3 と依存関係あり）

---

## 1-3. Web版でエラーメッセージが表示されない問題を直す

**背景**
`react-native-web` の `Alert.alert` は no-op のため、**Web版ではエラーが一切表示されない**。この認識は既に `apps/mobile/src/lib/confirm.ts` のコメントに書かれており、確認ダイアログは `confirmAsync` で対処済み。しかし通知用の `Alert.alert` が6箇所そのまま残っている。

該当箇所:
- `apps/mobile/app/poke-lids/[id].tsx` … アップロード失敗 / メモ保存失敗 / 削除失敗 / 権限エラー×2
- `apps/mobile/app/(tabs)/settings.tsx` … アカウント削除失敗

Web版で写真アップロードが失敗すると、スピナーが止まるだけで成功か失敗か判別できない。**このアプリで最も重要な操作**でこれが起きている。

**変更内容**
- `apps/mobile/src/lib/notify.ts` を新規作成し、`confirm.ts` と同じ流儀でプラットフォーム分岐した通知関数を用意する
- 上記6箇所を置き換える
- 可能なら `window.alert` ではなく画面内トースト（成功・失敗の両方に使える）にする。既存の `CelebrationModal` と表現を揃えること

**受け入れ基準**
- Web版で意図的にアップロードを失敗させたとき、ユーザーにエラー内容が見える
- `Alert.alert` の直接呼び出しがアプリコードから無くなる

---

## 1-4. 入力値の上限を設ける

**背景**
`apps/api/src/routes/collections.ts` の zod スキーマで、
- `notes` が `z.string()` のみで長さ無制限（multer のフィールド上限 1MB まで通る）
- `visitedAt` が `z.string().datetime()` のみで、西暦1000年でも2999年でも通る

`visitedAt` が異常値だとコレクション画面の「最初の記録 / 最新の記録」統計と `orderBy` の並びが壊れる。

**変更内容**
- `notes` に `.max(1000)`
- `visitedAt` に範囲チェック。下限はポケふた第1号の設置時期（2018年12月）、上限は「現在 + 1日」（クライアントの時計ずれを許容）
- POST と PATCH の両方に適用すること

**受け入れ基準**
- 範囲外の値で 400 と日本語エラーメッセージが返る
- 範囲判定ロジックは `packages/shared` に切り出し、境界値のテストを書く

---

## 1-5. CI にマイグレーション検証を追加する（対応済み・2026-08-08）

**背景**
2026-08-08、`20260806121231_photo_exif_distance` が本番デプロイ時に失敗し、APIコンテナがクラッシュループに陥った。原因はリポジトリに存在しないマイグレーション `20260730133315_photo_exif_distance_only` が7/30にサーバー上で直接適用されており、同じ変更を二重に行おうとしたこと。開発環境に Postgres がないため全マイグレーションが `prisma migrate diff` による手作りで、この不整合は push 時点で検出できなかった。

**変更内容**
`.github/workflows/ci.yml` の `typecheck-lint` ジョブに Postgres 16 のサービスコンテナ（health check付き）を追加し、以下2つの検証ステップを既存ステップの前に追加した。
- `prisma migrate deploy`：空DBに全マイグレーションを順番適用できるか
- `prisma migrate diff --from-url ... --to-schema-datamodel ... --exit-code`：適用後のDBと `schema.prisma` に差分がないか（差分があれば非ゼロ終了）

**受け入れ基準**
- ✅ 意図的に壊したマイグレーション／未マイグレーションのスキーマ変更の両方で、対応するCIステップが実際に落ちることを確認した（後者はローカルでPostgres 16コンテナを立てて `migrate deploy` → `migrate diff --exit-code` を再現し確認。前者は検証中に実際に発生：`20260808100000_add_photo_original_filename` が空DB適用時に「列が既に存在する」で失敗するバグを本番修正の直後に検出し、`IF NOT EXISTS` 付きに直した）
- ✅ 既存の typecheck / lint / prettier / test ステップは維持

---

## 1-6. /health をスキーマの健全性まで見るようにする（対応済み・2026-08-08）

**背景**
2026-08-08、`photos.original_filename` 列が本番DBに存在しないまま
デプロイされ、ログイン中のユーザーの全機能（地図・コレクション・
進捗・アップロード）が 500 で停止した。

しかし `/health` は `SELECT 1` しか実行しないため 200 を返し続け、
UptimeRobot の外形監視も正常と判定していた。
発見はユーザーからの報告によるもので、監視は機能しなかった。

**変更内容**
- `apps/api/src/index.ts` の `/health` を、`prisma.photo.findFirst()` /
  `prisma.collection.findFirst()`（`select` なし）を並行実行する形に変更。
  `select` を付けないことが必須で、これにより Prisma が全マップ済み列を
  SELECT するため、欠落・リネームされた列があればここで例外になる
- 全テーブルを舐める必要はない。Photo と Collection に触れば
  今回の種類の不整合は検出できる

**当初案からの訂正**
最初は `prisma.photo.findFirst({ select: { id: true } })` という案だったが、
これは `id` 列しか SELECT しないため、他の列（今回の
`original_filename` 相当）が欠けていても例外にならず検出できないことが
検証で判明した。`select` なしの `findFirst()` に変更して解決した。

**受け入れ基準**
- ✅ 検証用DB（`ci_test_postgres`）で `photos.original_filename` 列を
  リネームし、`select` なし版は 503 相当の例外になること、
  `select: { id: true }` 版は誤って 200 のまま通過してしまうことの
  両方を確認した
- ✅ 正常時のレスポンスタイムを計測し、30〜40ms程度で実用上問題ない
  ことを確認した（`/health/deep` への分離は不要と判断）

---

# フェーズ2: データモデル

## 2-1. 撤去されたポケふたを論理削除で扱えるようにする

**背景**
ポケふたは現実に撤去される（工事・破損・施設閉鎖）。現在の ETL は upsert のみなので、公式サイトから消えたポケふたが **DB に永久に残り、分母に含まれ続ける**。結果、誰も 100% を達成できず、存在しない場所へユーザーを案内し続ける。

かといって物理削除はできない。`Collection` が `onDelete: Cascade` なので、ポケふたを1件削除すると**それを収集した全ユーザーの記録と写真レコードが消える**（ファイルは孤児として残る）。

**変更内容**

1. スキーマに `retiredAt DateTime? @map("retired_at")` を追加し、インデックスを張る
2. ETL に差分検出を追加。今回のスクレイプ結果に含まれない `officialRef` を持つ既存レコードに `retiredAt` を立てる（**1-1 の安全装置が入っていることが前提**）
3. 集計・表示の扱いを変える:
   - 進捗の**分母は `retiredAt: null` のみ**
   - 一覧・地図・`/nearby` からは除外
   - ただし**既に収集済みの場合は「撤去済み」バッジ付きで表示する**（消さない）
4. 一度 `retiredAt` が立った後、再びスクレイプ結果に現れたら `null` に戻す（誤検出からの復帰）

**受け入れ基準**
- 撤去されたポケふたを収集済みのユーザーが、コレクション画面でその記録を引き続き見られる
- 進捗が 100% に到達可能である
- 撤去 → 復活 の往復でデータが壊れない
- 判定ロジックのテストがある

**補足**
「もう無くなったポケふたを持っている」ことは収集家にとって価値のある記録なので、消さずに残す方針を必ず守ること。

---

## 2-2. 写真の個別削除と主写真の変更を実装する

**背景**
現在、写真関連のエンドポイントは「アップロード」しかない。個別写真の削除がなく、`DELETE /api/collections/:id` で**記録ごと消す**しかない。

また `isPrimary` は `existingPhotoCount === 0` の1枚だけ true になる設計で、
- 主写真を後から変更できない（ベストショットを撮り直しても一覧に出ない）
- DB 側に一意性の保証がない

さらに、主写真を含む記録を消さずに主写真だけ削除する手段がないため、将来削除機能を足したときに「主写真が不在」の状態が発生しうる。その場合 `primaryPhoto ? photoUrl(...) : lid.officialImageUrl` のフォールバックにより、**ユーザーの写真があるのに公式イラストが表示される**という分かりにくい状態になる。

**変更内容**

1. エンドポイントを2本追加
   - `DELETE /api/collections/:id/photos/:photoId`
   - `PATCH  /api/collections/:id/photos/:photoId`（`{ isPrimary: true }`）
2. 削除時、それが主写真なら**残りのうち最も古い写真を自動的に主写真へ昇格**させる
3. 主写真変更は、旧主写真の解除と新主写真の設定を**同一トランザクション**で行う
4. DB 制約を追加。Prisma は部分ユニークインデックス未対応なので、マイグレーションに生 SQL を書く:
   ```sql
   CREATE UNIQUE INDEX one_primary_per_collection
     ON photos (collection_id) WHERE is_primary;
   ```
5. 削除時はファイル実体（本体とサムネイル）も消す。**DB 削除より先にファイル削除を試み、失敗してもログを残して処理を続行**すること（2-5 と同じ考え方）
6. 詳細画面に、写真ごとの「削除」「主写真にする」UI を追加

**受け入れ基準**
- 所有者チェックがある（他ユーザーの写真を削除・変更できない。404 で存在を隠す既存方針に合わせること）
- 主写真を削除しても、残り写真がある限り一覧に公式イラストが出ない
- 部分ユニークインデックスが効いており、複数 true にできない

---

## 2-3. EXIF の GPS 座標を DB に保存しない

**背景**
写真ファイル自体は sharp の再エンコードで EXIF が除去されている（良い実装）。しかし `Photo.exifLatitude` / `exifLongitude` として**生の座標が DB に永続化されている**。

メダル判定はアップロード時に完了しており、その後この座標を使う用途がない。ライブラリからの写真選択に対応したため、**ユーザーが自宅で撮った写真の座標が永久に DB に残る**状態になっている。ユーザーはそれを知らない。

**変更内容**
- `exifLatitude` / `exifLongitude` を削除し、`exifDistanceMeters Float?` に置き換える
- 判定の根拠説明（「なぜ銀メダルだったか」）は距離だけで足りる
- 既存データを移行するマイグレーションを書く（既存レコードは距離を再計算して埋め、座標列を drop する）
- プライバシーポリシー（`apps/mobile/app/privacy.tsx`）の記述も合わせて更新する

**受け入れ基準**
- マイグレーション後、`photos` テーブルに緯度経度の列が存在しない
- 既存の収集記録のメダルが変わらない
- プライバシーポリシーの記述と実装が一致している

---

## 2-4. インデックス追加と未使用フィールドの整理

**変更内容**

追加するインデックス:
```prisma
model Collection {
  @@index([userId, visitedAt])   // orderBy: { visitedAt: 'desc' } 用
}
model Photo {
  @@index([userId])              // 容量集計の aggregate 用（1-2 で使う）
}
```

未使用フィールドの調査と対応:
- `Prefecture.displayOrder` … `id` と同値を入れているだけ。使うか削るか判断すること
- `PokeLid.notes` … ETL でも API でも設定・参照されていない
- `PokeLid.installDate` … スキーマにあるが ETL が埋めていない。**公式サイトから取得できるなら埋める**（「2026年設置」のような絞り込みに使える）
- `User.isAdmin` … seed で設定できるが、管理者専用の機能もチェックも存在しない。**使う予定がないなら削除する**（将来「管理画面がある」と誤認する材料になるため）

**受け入れ基準**
- 削除する場合、マイグレーションが既存データを壊さない
- 残す場合、なぜ残すかをスキーマのコメントに書く

---

## 2-5. アカウント削除の順序を直す

**背景**
`apps/api/src/routes/auth.ts` の `DELETE /me` が、DB 削除 → ファイル削除の順になっている。`fs.rm` が失敗すると:
- ユーザーとレコードは消えている
- **GPS 情報を含む写真ファイルがディスクに残る**
- 参照する DB レコードがないので後から特定・再試行できない
- 500 が返り、クライアントは「削除に失敗しました」と表示するが**実際には削除されている**

**変更内容**
- ファイル削除を先に試み、失敗しても**握りつぶしてログにユーザーIDとパスを残し**、DB 削除は続行する
- レスポンスは 204 を返す（DB 削除が成功していれば成功として扱う）
- 孤児ファイルを後から回収できるよう、ログ形式を検索しやすくする

**受け入れ基準**
- ファイル削除に失敗しても、ユーザーには成功として応答が返る
- ログから孤児ディレクトリのパスが特定できる

---

# フェーズ3: UI / レイアウト

## 3-1. グリッド最終行のカードが引き伸ばされる問題を直す

**背景**
`PokeLidCard` が `card: { flex: 1 }` で、`FlatList` の `numColumns={3}` と組み合わさっている。React Native の `numColumns` は各行を row の View で包むため、**最終行の要素数が3未満だと `flex: 1` が余白を全部吸収する**。画像は `aspectRatio: 1` なので幅が3倍になれば高さも3倍になる。

影響:
- 都道府県詳細（件数が3の倍数でない県すべて）
- コレクション画面 … **記録が1件のとき必ず発生する＝新規ユーザーが最初の1件を記録した直後に確実に踏む**

**変更内容**
- データ配列を列数の倍数までプレースホルダー（`null`）で埋め、`renderItem` で空の `View`（`flex: 1` + 同じ padding）を返す
- `keyExtractor` がプレースホルダーで落ちないようにする
- 同じ処理が2画面に必要なので、フックかユーティリティに切り出すこと

**受け入れ基準**
- コレクション画面に記録が1件だけのとき、カードが1列分の幅で表示される
- 各都道府県詳細で最終行が崩れない

---

## 3-2. グリッドの列数を画面幅に追従させる

**背景**
`GRID_COLUMNS = 3` がハードコードされ、`useWindowDimensions` も `Dimensions` もコードベースに一度も登場しない。

実測:
| 環境 | カード幅 |
|---|---|
| iPhone SE (320px) | 約93px … **イラストの細部が判別できない** |
| iPhone 15 (393px) | 約118px |
| PC（maxWidth 720px） | 約227px … 481件を3列で並べると161行 |

**変更内容**
- `useWindowDimensions` で列数を切り替える（目安: 600px以上→5列、380px以上→3列、それ未満→2列）
- `FlatList` には既に `key={GRID_COLUMNS}` が渡してあるので `key={columns}` に置き換えれば `numColumns` 変更時の再マウントは動く
- 3-1 のプレースホルダー処理と整合させること

---

## 3-3. フィルタのタップ領域を広げる

**背景**
地図タブと都道府県詳細のフィルタが `<Text onPress>` で実装されている。
- タップ領域が文字の外接矩形＋`paddingVertical: spacing.xs`(4px) のみで、**実測の高さは約24px**。iOS/Android のガイドラインが求める44pxを大きく下回る
- `accessibilityRole="button"` がなく、スクリーンリーダーがただのテキストとして読む
- PC版で Tab キーでフォーカスできない
- `overflow: 'hidden'` + `borderRadius` を `Text` に付けているため、ネイティブで挙動差が出る

同じ問題が `settings.tsx` の `<Link style={styles.link}>` にもある。

**変更内容**
- `Pressable` に置き換え、`minHeight: 44` または `hitSlop` を付ける
- `accessibilityRole="button"` と `accessibilityState={{ selected }}` を付ける
- 再利用できるよう `FilterChip` コンポーネントとして切り出す

---

## 3-4. PC版でヘッダーとタブバーだけが全幅に伸びる問題を直す

**背景**
`ScreenContainer` の `maxWidth: 720` はコンテンツにのみ適用され、`Tabs` / `Stack` のヘッダーとタブバーはナビゲーター側が描画するため画面幅いっぱいに広がる。1920px のモニターでは4つのタブアイコンが中央付近に等間隔で並び、間隔が異様に広くなる。

**変更内容**
`apps/mobile/app/(tabs)/_layout.tsx` の `tabBarStyle` / `headerStyle` に `Platform.select({ web: { maxWidth: 720, alignSelf: 'center', width: '100%' } })` を適用する。

**代替案の検討**
Web版は SEO 流入の入口になるため、`tabBarPosition: 'top'` にして上部横並びナビにするほうが「Webサイト」として自然に見える可能性がある。両方試して判断すること。

**注意**
地図画面（`map.web.tsx`）は成功時に `ScreenContainer` を使わず全幅で表示している。これは**意図的に正しい**ので 720px に閉じ込めないこと。ローディング/エラー時だけ `ScreenContainer` を使っていて不整合なので、そちらを全幅の中央寄せに揃える。

---

## 3-5. 固定幅とフォント拡大への対応

**背景**
- ホームのヒーローが `heroCount`(fontSize: 40) + 単位 + パーセントバッジを1行に並べている。RN の `Text` はデフォルトで `allowFontScaling: true` なので、**OS のフォント拡大設定を最大にすると溢れる**
- 都道府県行のプログレスバーが `width: 110` 固定。320px 端末では窮屈、720px では小さすぎる
- コレクション画面の統計カードが2列固定。320px 端末では半分が約130pxで、「一番遠くまで行った記録」＋「稚内市（1,203.4km）」が破綻する

**変更内容**
- ヒーローに `flexWrap: 'wrap'` を入れるか、パーセントバッジを次行に落とす
- プログレスバーを `flex: 1, maxWidth: 160, minWidth: 80` の可変指定にする
- 統計カードを狭い画面では1列に落とす
- iOS 実機でフォントサイズ最大にして確認すること

---

## 3-6. 日付をJST固定にする

**背景**
`toLocaleDateString('ja-JP')` は**実行環境のタイムゾーン**を使う。静的レンダリング時や海外からのアクセスでズレる。このアプリの日付は「日本のどこで、いつ訪れたか」なので常に JST が正しい。23時に記録した分が UTC では翌日になる。

**変更内容**
- 日付整形を `{ timeZone: 'Asia/Tokyo' }` 固定のユーティリティに集約する
- `poke-lids/[id].tsx`（2箇所）、`collection.tsx`（3箇所）を置き換える
- コレクション画面の「最初の記録 / 最新の記録」の日付境界判定も JST 基準にする

---

# フェーズ4: 機能追加

## 4-1. 検索機能を追加する

**背景**
`TextInput` が一覧画面に一つもない。481件を都道府県で絞る以外の到達手段がない。`pokemonFeatured String[]` というデータを持っているのに、アプリ内で絞り込みにも表示にも使われていない。

収集家の実際の探し方:
- **ポケモン名** … 「ピカチュウのポケふたを全部集めたい」は主要なモチベーション
- **市町村名** … 「今いる市にポケふたある？」

**変更内容**
- `GET /api/poke-lids` に `?q=` と `?pokemon=` を追加
- `pokemonFeatured` に GIN インデックスを張る
- 一覧画面と地図に検索 UI を追加（デバウンスすること）
- 検索結果が0件のときの空状態を用意する

---

## 4-2. ポケモン別図鑑ビューを追加する

**背景**
既存データだけで**都道府県とは別の収集軸**が1本増える。「ピカチュウが何箇所にいて、そのうち何箇所集めたか」はリテンションに直結する。費用対効果が最も高い機能追加。

**変更内容**
- `GET /api/pokemon` … 登場するポケモンの一覧と、各ポケモンのポケふた総数
- ログイン時は収集済み数も返す
- 一覧 → ポケモン詳細（そのポケモンのポケふた一覧）の画面を追加
- 既存の図鑑メタファー（未収集グレースケール）をそのまま踏襲すること

**注意**
`pokemonFeatured` は ETL が `.zukan li a span` から偶数番目のみ抽出している。表記ゆれ（フォルム違い等）がありうるので、実データを確認してから正規化の要否を判断すること。

---

## 4-3. 新着ポケふたを表示する

**背景**
ポケふたは継続的に新設される。収集家にとって「新しいポケふたが増えた」は最も知りたい情報だが、現在それを知る手段がなく静かに追加されるだけ。

**変更内容**
- `PokeLid.createdAt` を使い、直近30日以内に追加されたものを「新着」として扱う
- ホーム画面に「新着ポケふた」セクションを追加
- `PokeLidCard` に NEW バッジ
- 2-4 で `installDate` を埋められた場合は、設置日ベースの表示も検討する

---

## 4-4. アップロード体験を改善する

**背景**
`setUploading(true)` でボタンがスピナーになるだけ。最大25MBを不安定なモバイル回線で送るため数十秒かかることがあり、何も進まない見た目だとユーザーは二重にタップする。

**変更内容**

1. **進捗表示** … `fetch` では進捗が取れないので `expo-file-system` の `createUploadTask` か `XMLHttpRequest` の `onprogress` に変える
2. **撮影後のプレビュー** … 現在は撮影後すぐアップロードが始まる。ブレていても指が写っていても送信される。「これで記録する / 撮り直す」の確認を挟む
3. **メダルが `NONE` のときのトーン** … `'NONE'` は truthy なので祝福モーダルが出る。現地まで行ったのに GPS がずれていた場合に祝福されるのは残酷。落ち着いたトーンにするか、再撮影を案内する
4. **リクエスト削減** … アップロード後に `fetchMyCollections` と `fetchPrefectureProgress` を続けて呼んでいる。アップロードのレスポンスに `collectedCount` と当該県の `collected/total` を含めれば1往復で済み、演出の表示も速くなる

---

## 4-5. ゲスト記録同期の結果をユーザーに伝える

**背景**
`syncGuestCollectionsToAccount` は `Promise.allSettled` で並列化され、失敗分だけをローカルに残す良い実装になっている。しかし `auth.tsx` の呼び出し側が**戻り値を捨てている**。

20件中3件が失敗してもユーザーには何も伝わらない。「保存しますか？」に「はい」と答えたのに17件しか入っていない。部分的に残ったローカルデータは次回ログイン時にまた聞かれるので、「同じダイアログが何度も出る」という不可解な挙動になる。

**変更内容**
- 成功件数と全件数を比較し、差があれば通知する（1-3 の `notify` を使う）
- メッセージに「残りは次回ログイン時に再試行します」と明記する

**任意（余裕があれば）**
`POST /api/collections/bulk` を作り、1トランザクションで投入する。原子性が得られ、写真なしの記録で `multipart/form-data` と multer を経由しなくて済む。ただし現実的な件数では現状の実装でも動くため優先度は低い。

---

## 4-6. 写真の重複・転載チェック

**背景**
インターネット上から拾った他人の写真をアップロードして「収集した」ことにする不正を防ぎたい。

ただし **「この画像がインターネット上に存在するか」を完全に判定することはできない**。リバース画像検索の実用的な API は Google Cloud Vision の Web Detection のみで、1,000ユニットあたり $3.50（無料枠は各機能月1,000ユニットまで）。加えてユーザーの写真を Google に送信することになる。

さらに、このアプリ特有の2つの難しさがある。

1. **誤検知しやすい題材である。** ポケふたは円形で、正面から中央に収めて撮るのが自然。別人が同じポケふたを撮った写真は知覚ハッシュがかなり近くなる。
2. **「ネット上にある」＝不正ではない。** ユーザーが自分で撮った写真を SNS に投稿してからアップロードするのは完全に正当。Web 検索ではこれも「存在する」と判定される。

したがって **二値の可否ではなく段階的な判定**にする。明確に黒のものだけ拒否し、グレーはフラグを立てて記録自体は通す。

**前提**
2-2（写真の個別削除・主写真変更）を先に実施していること。`Photo` テーブルを両方で触るため。

**変更内容**

### スキーマ

```prisma
enum PhotoOriginality {
  OK
  DUPLICATE          // 既存写真と同一
  OFFICIAL_ARTWORK   // 公式イラストそのもの
  FLAGGED            // 要確認（記録は有効）
}

model Photo {
  // ...
  sha256      String            // ファイルバイト列のハッシュ（完全一致検出用）
  phash       Bytes             // 知覚ハッシュ 64bit（8バイト）
  originality PhotoOriginality  @default(OK)

  @@index([sha256])
}
```

`phash` は `bytea` で保存し、比較は Postgres の `bit_count(phash # $1)` を使う（PG14以降が必要。現在 postgres:16 なので可）。写真数が少ないうちは全件取得して JS 側で計算しても構わないが、その場合はコメントに理由を書くこと。

### 判定パイプライン（アップロード時、リサイズ後の画像に対して実行）

上から順に評価し、最初に該当したものを採用する。

| 条件 | 結果 |
|---|---|
| SHA-256 が**他ユーザーの**既存写真と完全一致 | **拒否**（400） |
| 公式イラスト（`/api/official-images/`）と pHash 距離 ≤ 8 | **拒否**（400） |
| 同一ポケふたの他ユーザー写真と pHash 距離 ≤ 6 **かつ** `exifCapturedAt` が秒まで一致 | **拒否**（400） |
| 同一ポケふたの他ユーザー写真と pHash 距離 ≤ 6（撮影日時は不一致 or 不明） | `FLAGGED`・**メダルは強制 NONE**・記録は有効 |
| SHA-256 が**自分の**既存写真と一致 | **拒否**（400、「同じ写真が既に登録されています」） |
| 上記いずれにも該当しない | `OK` |

**閾値を厳しめ（≤6）にしている理由**：同じポケふたを撮った正当な写真同士が近い距離になるため。距離だけで判断せず、`exifCapturedAt` の一致を併用する。**撮影日時が秒まで一致する2枚は、別人が撮った別の写真ではありえない。** これが最も信頼できる決め手になる。

### 公式イラストのハッシュ

ETL の `downloadOfficialImage` で保存する際に pHash を計算し、`PokeLid` に `officialImagePhash Bytes?` として持たせる。アップロードのたびに画像ファイルを読み直さない。

### 拒否時のメッセージ

理由ごとに日本語で明確に返す。「不正」と断定する文言は避け、事実だけを述べること（誤検知の可能性があるため）。

- 「この写真は公式のイラスト画像と一致します。現地で撮影した写真を登録してください。」
- 「この写真は既に別の記録で使用されています。」
- 「同じ写真が既にこの記録に登録されています。」

### Web Detection（任意・後回しでよい）

Google Cloud Vision の `WEB_DETECTION` で `fullMatchingImages` が返った場合に `FLAGGED` を立てる。**拒否には使わない**（ユーザー自身の SNS 投稿を弾いてしまうため）。

導入する場合:
- プライバシーポリシーに「不正防止のため画像を外部サービスに送信する場合がある」旨を明記する
- 無料枠（月1,000件）を超えたら自動的にスキップする安全弁を入れる
- 環境変数で機能ごとオフにできるようにする

**受け入れ基準**
- 公式イラストをそのままアップロードすると拒否される
- 他ユーザーの写真ファイルを入手して同一バイト列でアップロードすると拒否される
- **同じポケふたを別々に撮影した2枚の正当な写真が、誤って拒否されない**（テストで担保すること）
- pHash 計算と距離判定は `packages/shared` に切り出し、ユニットテストを書く
- 判定ロジックは既存のメダル判定（`determinePhotoMedal`）と同じ流儀で実装する

**設計上の注意**
既存のメダル制度が既にこの役割を部分的に担っている。ネットから拾った画像は SNS が EXIF を除去するため位置情報がなく、GOLD にならない。この機能はゼロから作るのではなく、**メダル制度の補強**として位置づけること。

`FLAGGED` になった写真をどう扱うか（管理者に通知する、ユーザーに見せる、集計から除外する）は、実際に運用してフラグの発生頻度を見てから決める。**最初は記録するだけにして、UI には出さないこと。** 誤検知率が分からないうちにユーザーへ表示すると、正当なユーザーを不快にさせる。

---

## 5-1. 詳細ページを静的生成する

**背景**
`app.json` は `output: "static"` だが、動的セグメント `[id]` は `generateStaticParams` がないと生成されない。現在 `/poke-lids/{id}` 481件と `/prefectures/{id}` 47件は静的化されておらず、`app.get('*')` のフォールバックが全 URL に同じシェルを返している。sitemap.xml で528件を申告済みなので、**クローラは来るのに中身が空**の状態。

**重要な注意**
`generateStaticParams` を入れるだけでは効果がほぼない。詳細画面は `useEffect` で fetch しているため、ビルド時レンダリングでは `lid` が `null` で、`Head` のフォールバック側タイトルが全ページに焼き込まれてしまう。

**変更内容（この2つはセットで実施すること）**

1. ETL のスナップショット JSON をビルドに含め、詳細画面の初期値として同期的に使う
   ```tsx
   import POKE_LIDS from '../../src/data/poke-lids.json';
   const staticLid = POKE_LIDS.find((l) => l.id === id) ?? null;
   const [lid, setLid] = useState<PokeLidDto | null>(staticLid);
   ```
   収集状態だけを API から取りに行く形にする。
2. 両方の動的ルートに `generateStaticParams` を実装する（API を叩かず JSON を読む。ビルド時に API が起動しているとは限らないため）

**副次的な効果**（これも狙う価値がある）
- 詳細ページが API を待たずに即座に描画される
- オフラインでも詳細ページが開ける

**受け入れ基準**
- ビルド後、`dist/poke-lids/<id>.html` が生成されている
- 生成された HTML の `<title>` がポケふたごとに異なる
- バンドルサイズの増加を確認し、許容範囲か判断する

---

## 5-2. パスワードリセットを実装する

**背景**
パスワードを忘れたユーザーの復帰手段が**ゼロ**。アカウント削除して作り直すしかなく、収集記録が全部消える。旅の記録という性質上、実質的なデータ損失。

**変更内容**

1. スキーマに `PasswordResetToken` を追加。**既存の `RefreshToken` と同じ設計パターンを踏襲する**（`tokenHash` で保存、`expiresAt`、`usedAt`、`onDelete: Cascade`）
2. メール送信手段を追加（Resend / Amazon SES など）。独自ドメインなので SPF/DKIM/DMARC の設定が必要
3. エンドポイント2本
   - `POST /api/auth/password-reset/request` … `{ email }`
   - `POST /api/auth/password-reset/confirm` … `{ token, newPassword }`

**セキュリティ要件（必ず守ること）**
- `/request` は**登録済みかどうかに関わらず常に同じレスポンス**を返す（アカウント列挙対策）
- 既存の `credentialRateLimit` を `/request` にも適用する（メール爆撃の踏み台対策）。IP だけでなくメールアドレス単位の制限も検討
- 有効期限は1時間程度。`usedAt` を立てて単回使用にする
- トークン比較は `timingSafeEqual`（`verifyPhotoAccessToken` の手法を流用）
- **リセット成功時に、そのユーザーの全リフレッシュトークンを revoke する**。これを忘れると「パスワードを変えたのに乗っ取った側のセッションが生きたまま」になる。`user.update` / `token.update` / `refreshToken.updateMany` を同一トランザクションで実行すること

**テスト（`auth.test.ts` と同じ形で）**
- 期限切れトークンの拒否
- `usedAt` 済みトークンの再利用拒否
- 他ユーザーのトークンでの変更試行
- リセット後に旧リフレッシュトークンが無効になっていること

**関連**
登録時のメールアドレス確認が未実装なので、他人のアドレスで登録された場合にリセット機能で乗っ取られうる。メール送信基盤は共通なので、**このタスクと同時に登録時確認も入れるのが効率的**。ただし優先度はリセットが先（既存ユーザーの救済手段がないほうが緊急度が高い）。

---

## 5-3. docker-compose を堅牢化する（対応済み・2026-08-14）

**背景**
自宅サーバーでの運用中に、以下の弱点がある。2026-08-08 にこの項目の1番目が未対応だったために実害が出た（`original_filename` のマイグレーション失敗で API がクラッシュループしサイトが停止）。CI（1-5）で事前に防ぎ、`/health`（1-6）で検知する仕組みはあったが、「被害を抑える」層だけが欠けていた。

**変更内容**

1. **マイグレーションをアプリ起動から切り離す**
   `apps/api/Dockerfile` の `CMD` を `node dist/index.js` のみにし、`prisma migrate deploy` は `docker-compose.yml` の一回限りサービス `pokelids_migrate` に分離。`pokelids_api` は `depends_on: pokelids_migrate: condition: service_completed_successfully` で待つ。`npx` ではなく `/repo/node_modules/.bin/prisma` を直接呼ぶ。
   **`pokelids_migrate` のコマンドは `prisma migrate deploy` が失敗してもコンテナ自体は exit 0 で終える**（`|| { echo ...; exit 0; }`）。`service_completed_successfully` は依存先が非ゼロ終了すると依存元を起動させない（実際にこの構成で確認済み）ため、素直に実装すると「マイグレーション失敗時はAPIも起動しない」という、今回防ぎたかった全停止の別形になってしまう。失敗は `docker compose logs pokelids_migrate` と既存の `/health` 503（1-6）で拾う。
2. **ログローテーション**（`pokelids_api`・`pokelids_migrate`・`pokelids_postgres` 全サービス）
   `logging: { driver: json-file, options: { max-size: '10m', max-file: '3' } }`
3. **メモリ上限**：`pokelids_api` に `mem_limit: 1g`。5枚同時処理の bulk API（7-9 フェーズ2）に25MB級の画像5枚を実際に投げて `/sys/fs/cgroup/memory.current` を20〜50ms間隔で測定し、ピーク約250MB・定常状態約200MB（リークなし）を確認した上で、約4倍の余裕を見て1gに決定。
4. **ヘルスチェック**：`/health` を Docker の `healthcheck` に接続。**Docker の再起動ポリシーは `unhealthy` を見ないため**、`willfarrell/autoheal` を追加（`autoheal=true` ラベルを付けた `pokelids_api` のみ対象、`pokelids_postgres` は対象外）。systemd タイマーではなくこちらを選んだ理由は `docker-compose.yml` のコメントに残した。
5. **グレースフルシャットダウン**（`apps/api/src/index.ts`）：`SIGTERM`/`SIGINT` で `server.close()` → `prisma.$disconnect()`、10秒フォールバック付き。
6. **バックアップ先の分離**：`scripts/backup.sh` の `BACKUP_ROOT` を `/home/setoyama/pokelids-backups`（Postgresと同じ root LVM、当時84%使用・16GB空き）から `/mnt/photos/pokelids-backups`（RAID6、1.7TB空き）に変更。既存データは `rsync -aH` でハードリンク関係を保ったまま移行し、旧ディレクトリは削除、crontab のログ出力先も更新済み。

**受け入れ基準**
- ✅ `docker compose up -d` で4サービス（api/migrate/postgres/autoheal）すべて起動し、`pokelids_api`・`pokelids_postgres`・`pokelids_autoheal` が healthy になることを確認した
- ✅ **（最重要）** 本番DBに触れない使い捨てのDocker環境を用意し、構文的に不正なSQLを含むマイグレーションを実際に適用させて失敗させた上で、`pokelids_migrate` は exit 0、`pokelids_api` は正常に起動してHTTPリクエストに応答することを確認した（`docker compose logs` に実際のPrismaエラー`P3018`が出力されることも確認）
- ✅ 本番の `pokelids_api` に対し、5枚・約23MBずつの画像を送る bulk アップロード（処理に約2.3秒かかる）の最中に `docker compose restart pokelids_api` を実行し、そのリクエストが201で正常完了すること、ログに `SIGTERM received, shutting down gracefully` が出ることを確認した
- ✅ 外部ドメイン（`https://pokelids-collect.jp/`・`/map`・`/health`）が全て正常応答することを確認した
- ✅ `scripts/backup.sh` を新しいデフォルトパスで手動実行し、正常完了することを確認した

---

## 5-4.（任意）写真を Cloudflare R2 に移す

**背景**
2つの理由がある。

1. **規約** … Cloudflare のセルフサービス利用規約 第2.8項は、無料/Pro/Business プランで HTML 以外のコンテンツ（大量の画像等）を不当に多く配信することを禁じている。現在 `/api/photos/` からユーザー写真を Tunnel 経由で配信しており、ユーザー増加に伴って抵触しうる。
2. **可用性** … 写真と Postgres が同一ディスクにあるため、写真でディスクが埋まると DB が停止する（5-3 の6と同根）。

R2 の無料枠は10GB・エグレス無料で期限なし。リサイズ後1枚1MB程度なので約1万枚。

**変更内容**
- `filePath` を R2 のオブジェクトキーに読み替える
- `signPhotoAccessToken` を R2 の署名付き URL 生成に差し替える（**既存の仕組みとほぼ1:1で対応する**ので移行コストは低い）
- 変更範囲は `apps/api/src/routes/collections.ts` と `photos.ts` の2ファイルで収まるはず
- 既存写真の移行スクリプトを別途用意する

---

# フェーズ6: 使い勝手の向上（UX）

機能は揃っているが「初見で分からない」「やりたいことに機能が対応していない」領域。

## 6-1. 初回オンボーディングと権限リクエストの文脈化

**背景**
初回起動でいきなりホーム画面が出る。以下が**一切説明されない**。

- ログインしなくても記録できること（ゲスト記録）
- ゲスト記録は端末に保存されており、ログインすれば引き継げること
- 🥇🥈 メダルが何を意味するか（現在は設定画面の奥にのみ凡例がある）

また位置情報の権限を、**なぜ必要かを説明しないまま OS のダイアログで要求している**。一度拒否されると設定アプリから変更してもらうしかなく、ホーム画面の「次に集めよう」が常に北海道から表示される状態が続く。位置情報はこのアプリの中核機能なので、ここでの取りこぼしは大きい。

**変更内容**

1. 初回起動時に3枚程度の簡潔な説明を表示する。スライドショーは飛ばされやすいので、**1画面に要点をまとめた形**でもよい。伝えるべきは上記3点のみ。長くしないこと。
2. OS の権限ダイアログを出す**前に**、なぜ位置情報が必要かを自前の画面で説明する（プリパーミッション）。「近くのポケふたを表示するために使います。位置情報は端末外に送信されません」など、事実を正確に。
3. 拒否された場合、ホーム画面に「位置情報を許可すると近くのポケふたが表示されます」という導線を出し、`Linking.openSettings()` へ繋ぐ。
4. 表示済みフラグは AsyncStorage に保存する（ゲスト記録と同じ流儀）。

**受け入れ基準**
- 2回目以降の起動でオンボーディングが出ない
- 位置情報を拒否した状態でも、許可への導線が画面上に存在する
- オンボーディングをスキップできる

---

## 6-2. 空状態を「次の行動」に繋げる

**背景**
コレクション画面の空状態が `<Text>まだ収集記録がありません</Text>` の一行のみ。**次に何をすればいいか分からない**。新規ユーザーが最初に見る画面のひとつなので、ここで離脱する。

同様の問題が、検索結果0件（4-1 実装後）、地図で「未収集のみ」に絞って0件、都道府県詳細で全収集済み、などにもある。

**変更内容**
再利用可能な `EmptyState` コンポーネントを作り、各画面に適切な文言とアクションを置く。

| 画面 | 文言の方向性 | アクション |
|---|---|---|
| コレクション（0件） | 最初の1枚を記録しよう | 「近くのポケふたを探す」→ 地図タブ |
| 都道府県詳細（全収集済み） | この都道府県はコンプリート | 「次の都道府県へ」 |
| 検索結果0件 | 該当なし | 検索条件をクリア |
| 地図・未収集0件 | この範囲はすべて収集済み | フィルタを「すべて」に戻す |

**受け入れ基準**
- すべての空状態に、次の行動へのボタンがある
- 文言が否定形で終わらない（「ありません」で止めない）

---

## 6-3. 行きたいリスト（旅行計画機能）

**背景**
現在のアプリは「見つけたものを記録する」ことしかできない。しかしポケふたコレクトは本質的に**旅行の計画を伴う活動**で、「今度神戸に行くから、神戸のポケふたを回りたい」が主要なユースケース。この機能が丸ごと欠けている。

**変更内容**

1. スキーマに追加
   ```prisma
   model Wishlist {
     id        String   @id @default(uuid())
     userId    String   @map("user_id")
     pokeLidId String   @map("poke_lid_id")
     createdAt DateTime @default(now()) @map("created_at")

     user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
     pokeLid PokeLid @relation(fields: [pokeLidId], references: [id], onDelete: Cascade)

     @@unique([userId, pokeLidId])
     @@index([userId])
     @@map("wishlists")
   }
   ```
2. ゲストでも使えるようにする（`guestStorage.ts` の収集記録と同じ方式。ログイン時に同期）
3. 詳細画面・カード長押しから「行きたい」のトグル
4. 地図のフィルタに「行きたいリストのみ」を追加
5. コレクションタブに「行きたい」セクション、または独立したタブ
6. **記録すると自動的に行きたいリストから外す**

**受け入れ基準**
- ゲスト状態で追加した行きたいリストが、ログイン時に引き継がれる
- 地図で行きたいリストだけを表示できる
- 収集済みのポケふたを行きたいリストに追加できない（またはUIに出ない）

**任意（後で）**
複数地点を選んで移動順に並べる簡易ルート機能。まずは上記だけで十分価値がある。

---

## 6-4. 都道府県一覧の並び替えと絞り込み

**背景**
47都道府県が地方順の固定表示のみ。「あと1つでコンプリートの県」を探すのに全部スクロールする必要がある。

**変更内容**
- 並び替え: 地方順（現状）/ 達成率が高い順 / 達成率が低い順 / 現在地から近い順
- 絞り込み: 未コンプリートのみ
- 選択状態を AsyncStorage に保存し、次回起動時も維持する
- 3-3 で作る `FilterChip` を再利用すること

**受け入れ基準**
- 「達成率が高い順」で、あと少しでコンプリートの県が上位に来る
- 設定した並び順がアプリ再起動後も維持される

---

## 6-5. プルトゥリフレッシュを全画面に統一する

**背景**
`onRefresh` が実装されているのはホーム画面のみ。コレクション画面・都道府県詳細・地図には引っ張って更新する手段がない。モバイルでは最も自然な更新操作なので、ある画面とない画面が混在すると混乱する。

**変更内容**
`FlatList` / `ScrollView` を持つ全画面に `refreshing` / `onRefresh`（または `RefreshControl`）を追加する。3-6（`useFocusEffect` 統一）と整合させ、更新処理を共通化できないか検討すること。

---

## 6-6. 地図から直接記録できるようにする

**背景**
現在、記録するには「地図でピンをタップ → 詳細画面へ遷移 → 写真を撮る」と3ステップ必要。現地でポケふたの前に立っている状況では、この遷移が煩わしい。

**変更内容**
- 地図のピンをタップしたときのポップアップに「写真を撮って記録」ボタンを追加する
- 現在地から一定距離内（例: 200m）のポケふたにのみ表示する。遠くのピンに出すと誤操作になる
- 記録後は詳細画面に遷移せず、その場で `CelebrationModal` を出して地図に留まる

**受け入れ基準**
- 地図から離脱せずに記録が完了する
- 遠隔地のピンにはこのボタンが出ない

---

## 6-7. 触覚フィードバックと達成の手触り

**背景**
記録成功時の演出が視覚のみ。屋外で画面を注視していない状況もあるため、触覚があると「記録できた」ことが確実に伝わる。

**変更内容**
`expo-haptics` を導入し、以下に割り当てる。

| 場面 | 種類 |
|---|---|
| 記録成功（GOLD） | `notificationAsync(Success)` + やや強めの impact |
| 記録成功（SILVER / NONE） | `notificationAsync(Success)` |
| エラー・拒否 | `notificationAsync(Error)` |
| フィルタ切り替え・タブ切り替え | `impactAsync(Light)` |

**注意**
- Web では `expo-haptics` が no-op になるので、Platform 分岐は不要だが動作確認はすること
- OS の「視差効果を減らす / 触覚を減らす」設定を尊重する。`AccessibilityInfo.isReduceMotionEnabled()` を見て `CelebrationModal` のアニメーションも簡略化すること

---

## 6-8. ダークモード

**背景**
`theme.ts` の色が固定値。ポケふたコレクトは夜の移動中や早朝にも使われる。トークンが1ファイルに集約されているため、対応コストは比較的低い。

**変更内容**
- `useColorScheme()` でライト / ダークを切り替える
- `theme.ts` を `lightColors` / `darkColors` に分け、Context かフックで供給する
- 設定画面に「システムに合わせる / ライト / ダーク」の選択を置き、AsyncStorage に保存する
- **図鑑のグレースケール表現がダークでも成立するか確認すること**（未収集が背景に溶けないよう、不透明度ではなく彩度を落とす方向で調整が必要かもしれない）
- 地図（Leaflet）のタイルもダーク時に切り替えるか検討する

**受け入れ基準**
- 全画面でコントラスト比が WCAG AA（4.5:1）を満たす
- ダーク時に未収集/収集済みの区別が明確につく

---

## 6-9. シェア機能

**背景**
「兵庫県コンプリート！」を SNS に投稿したいユーザーは多い。OGP 画像は既に用意されているのに、アプリ内から共有する手段がない。自然な流入経路を逃している。

**変更内容**
- `expo-sharing` / `Share` API を使い、以下から共有できるようにする
  - 収集記録の詳細（写真＋ポケふた名＋訪問日）
  - 都道府県コンプリート時（`CelebrationModal` に共有ボタン）
  - コレクション全体のサマリー（訪問都道府県数・🥇数）
- 共有画像は `react-native-view-shot` で画面の一部をキャプチャする
- **共有画像に個人情報（正確な位置・自宅周辺が写った写真）が含まれないよう注意すること**

---

## 6-10. ナビゲーション連携と住所の実用性

**背景**
詳細画面に住所と「Googleマップで開く」があるが、実用性を上げる余地がある。

**変更内容**
- Googleマップのリンクを `?q=` から**経路案内**に変える
  `https://www.google.com/maps/dir/?api=1&destination=<lat>,<lng>`
- 住所をタップでコピーできるようにする（`expo-clipboard`）。カーナビへの手入力に使う
- iOS では Apple マップを選べるようにする（`maps://?daddr=`）。選択肢を出すか、OS 標準に委ねるか判断すること
- 現在地からの距離表示は実装済み。あわせて**徒歩/車の概算所要時間**を出せると判断材料が増える（直線距離からの粗い推定でよいが、推定であることを明示すること）

---

**1タスクずつ渡す。** このドキュメントの該当セクションをそのまま貼り、冒頭に「0. 共通ルール」を添えるのが基本形。

**依存関係に注意する。**
- 1-1（ETL 安全装置）→ 2-1（論理削除）… 逆順にすると全件が撤去扱いになる事故が起きうる
- 1-3（notify）→ 1-2 / 4-5 … エラーメッセージの表示先が必要
- 3-1（最終行）と 3-2（列数）… 同時にやるほうが整合を取りやすい
- 5-1 の2つの変更はセットでないと効果が出ない

**変更後は必ず実機で確認する。** 特に 3-x のレイアウト系は、iPhone SE 相当の狭い画面、OS のフォント拡大設定、PC ブラウザの3つで見ること。

**迷ったら聞かせる。** 「既存の設計と矛盾する箇所があれば、実装を進める前に指摘してください」と添えると、齟齬が早い段階で表面化する。

## 7-6. 収集記録をクライアント側で共有する（リクエスト削減③）

背景:
fetchMyCollections() をホーム・コレクション・地図・都道府県詳細・
ポケふた詳細の5画面すべてが呼んでいる。useFocusEffect により
タブを行き来するたびに再取得され、4タブ一巡で複数回叩かれる。

変更内容:
- collections を Context で共有し、初回のみ取得する
- 4-4 で各エンドポイントが最新の collection を返すようになるので、
  書き込み時はレスポンスで差し替える（再取得しない）
- 明示的な更新手段（プルトゥリフレッシュ・地図の更新ボタン）では
  再取得すること
- 写真トークンの有効期限が45分なので、それを超えて古いキャッシュを
  使い続けないこと

注意:
キャッシュは「古いデータを見せる」リスクを伴う。
別端末で記録した場合に反映されないので、更新手段は必ず残すこと。

---

## 7-7. ポケふたマスタをバンドル済みJSONから読む（リクエスト削減②）

背景:
fetchPokeLids()（引数なし＝全481件、数百KB）をコレクション画面と地図が
呼んでいる。ポケふたのマスタデータは静的なので、そもそも取得する必要がない。
apps/mobile/src/data/poke-lids.json が 2-1 の作業で既にバンドルされている。

変更内容:
- 全件取得をバンドル済みJSONの読み込みに置き換える
- ETL で追加・撤去された分に追従するため、軽量な版数チェックを設ける
    GET /api/poke-lids/version → { updatedAt: string }
  バンドル済みJSONより新しければ差分または全件を取りに行く
- 5-1（静的生成）でも同じJSONを詳細ページの初期値に使う計画なので、
  読み込み口を共通化しておくこと

効果:
コレクション画面と地図から、数百KBの取得が消える。
オフラインでも一覧と地図が開けるようになる。

---

## 7-10. ゲストの記録が端末から消えるリスクを下げる

**背景**

7-9（フェーズ1）でゲストが写真を撮って端末に保存できるようになったが、
端末保存にはブラウザの eviction リスクがある。

- Safari（iOS17+/macOS14+）… 7日間そのサイトへの操作がないと、スクリプトが
  作ったデータ（IndexedDB含む）を自動削除する
- Chrome/Firefox … ディスク逼迫時に LRU（最終アクセスが古いoriginから）で削除

ROADMAP.md の判断原則1「ユーザーの写真は二度と撮り直せない。データ保全は
他のあらゆる改善より優先する」に照らすと、iOSユーザーが1週間アプリを
開かないだけで、旅先で撮った写真が本人の気づかないまま消えうる。

実機フィードバックで「ログインしなくても記録できるならログインする気に
ならない」という指摘があったが、これは自然な感覚であり、Androidでは
実際に困らないため（Chromeの永続化はエンゲージメント指標で自動的に
付与されやすい）。したがって対処すべきは「ログイン率を上げる」ことでは
なく、**ゲストのままでも記録が失われないようにする**こと。

**変更内容**

1. **`navigator.storage.persist()` を要求する**
   付与されると origin のストレージが eviction 対象から外れる。ゲストが
   最初の写真を保存した直後（アプリ起動時ではなく、守るべきデータが
   できた瞬間）に要求する。`navigator.storage.persisted()` で既に
   付与済みか先に確認し、重複要求しないこと。非対応ブラウザ・ネイティブ
   では何もしない。付与はブラウザ側が自動判定するもので、ユーザーへの
   プロンプトは基本的に出ない（出るブラウザもある）。

2. **ホーム画面への追加を促す**
   永続化の付与を受けやすくすると同時に、再訪率にも効く。ゲストの記録が
   3件程度たまったタイミングで控えめに案内する。
   - Chrome/Edge は `beforeinstallprompt` イベントを捕まえてネイティブの
     インストールプロンプトを出せる
   - iOS Safari は `beforeinstallprompt` に対応していないため、
     「共有ボタン→ホーム画面に追加」の手順を文言で案内する
   - 一度閉じたら、しばらく（目安2週間）再表示しない

3. **永続化されていない場合のみ、コレクション画面のバナー文言を変える**
   `persist()` が拒否された、または非対応の場合のみ、ログインの利点を
   より具体的に伝える文言にする。付与されている場合（ネイティブ含む）は
   現行の文言のまま。**「消えます」という直接的な警告は出さない** —
   恐怖ではなく利点として提示すること。

**やらないこと**
- ログインの強制、ログインを促す表示の増加
- 「消えます」という直接的な警告

個人の収集記録アプリとして、ゲストのまま使い続ける人が多数派でも構わない。
守るべきはその人の記録であって、アカウント作成率ではない。

**受け入れ基準**
- 写真を1枚保存した後、`navigator.storage.persisted()` が true を返すか
  確認する（ブラウザによってはfalseのままだが、それも正常動作）
- 非対応ブラウザ・ネイティブでエラーが出ない
- ホーム画面追加の案内が、iOS SafariとAndroid Chromeそれぞれで
  適切な内容になっている
- コレクション画面のバナーが、永続化の有無で正しく出し分けられている
