# ポケふたコレクト — ロードマップ

TASKS.md は「何を直すか」の一覧。このドキュメントは **「なぜ、どの順で」** を決めるもの。
判断に迷ったらこちらを優先する。

---

## 1. このアプリは何か

> **行った証明が残る、日本を巡る図鑑。**

チェックリストアプリではない。差別化しているのは **メダル制度** ひとつに尽きる。

- 🥇 GOLD … 写真の EXIF 位置情報が現地と一致した = **その場所に行ったことが証明されている**
- 🥈 SILVER … 位置情報がない写真
- ⬜ NONE … 一致しなかった

「見た」ではなく「行った」を記録できることが、このアプリの存在理由。
**機能を足すときは、この芯を強くするかどうかで判断する。** 芯と無関係な機能は、
どれだけ便利そうでも後回しにしてよい。

---

## 2. コアループと、いま欠けているもの

**偶然を、目的に変える。** これがこのアプリの設計課題のすべて。

最初の1つは、たまたま見つけたものでいい。それは入口にすぎない。
そこから「**次は探しに行こう**」に変わったとき、このアプリは初めて成立する。

```
 ①偶然の発見  →  ②気づき  →  ③目的化  →  ④継続
   （導入）      （欠落）    （欠落）    （薄い）

              「他にもある」  「探しに行く」  「集め続ける」
```

**現状、①で止まっている。**

| 段階 | 必要なこと | 現状 |
|---|---|---|
| ①偶然の発見 | 見つけた場所を記録できる | **できている（ただしゲストは写真＝メダルを体験できていない。7-9で着手）** |
| ②気づき | 記録直後に「他にもある」と分かる | **できている**（7-4: `CelebrationModal` に次の候補を表示） |
| ③目的化 | **今日・今週行ける目標**が見える | **できている**（7-5: 「今日行ける範囲」＋市区町村の一部） |
| ④継続 | 集めた実感と、次の理由が返ってくる | 統計カードが3つ |

### ③目的化がいちばん難しい

②の「あと57個ある」は規模の情報でしかない。人が動くのは、**到達できる目標**があるとき。

当初は **市区町村単位** の粒度が効くと考えていた（`PokeLid.municipality` は既に
データとして持っているのに、集計にも表示にも使われていなかった）。「兵庫県
12/57」より「西宮市 3/4 — あと1つ」のほうが人は動くはずだ、という仮説である。

しかし 7-5 実装時に実データ（481件・399市区町村）を集計したところ、
想定（1〜4個程度）とは大きく違う結果になった。

| 市区町村あたりの件数 | 市区町村数 | 割合 |
|---|---|---|
| 1個のみ | 364 | 91.2% |
| 2個 | 13 | 3.3% |
| 3個以上 | 22 | 5.5% |

**9割の市区町村がそもそも1個しか持たない。** 1個しかない市区町村の
「あと1つ」は「まだ1個も集めていない」と区別がつかず、②の
`nearestUncollected`（一番近い未収集）と情報として同じになる。「あと1つ」が
本当に意味を持つのは、2個以上ある**35市区町村（全体の8.8%）**だけだった。

7-5 はこれを踏まえ、市区町村単位の目標は `total >= 2` の市区町村のみを対象にした
（祝福も同条件でガードしている。1個の市区町村は記録するたびに機械的に
「コンプリート」になり、頻発しすぎて祝福が安っぽくなるため）。

**③目的化の主役は、市区町村ではなく「今日行ける範囲」に置く。** 「現在地から
10km以内に未収集がN個」（7-5で実装済み）は市区町村の粒度に依存せず、
どのユーザー・どのエリアでも成立する。市区町村の「あと1つ」は、該当する
8.8%のエリアに効く**上乗せの動機**という位置づけにする。

図鑑のグレースケール表現は、既に②を部分的に担っている良い設計。
**穴が見えることが、埋めたくなる動機になる。** ③はその延長線上に作る。

---

## 3. 判断の原則

迷ったときはこの順で優先する。

1. **ユーザーの写真は二度と撮り直せない。**
   旅先で撮った写真を預かっている。データ保全は他のあらゆる改善より優先する。
   バックアップ、容量制限、削除の安全性。ここでの妥協はしない。

2. **屋外・片手・電波が弱い場所で使われる。**
   ポケふたは山間部や郊外に多い。直射日光下、移動中、圏外。
   → 通信を減らす。失敗を前提にする。タップ回数を減らす。コントラストを確保する。

3. **収集の楽しさが体験の核。**
   進捗の可視化・達成の演出への投資は、新機能より費用対効果が高い。
   グレースケール表現とメダルは既に良い。**これを磨くほうが、機能を足すより効く。**

4. **個人開発である。**
   維持コストの低い選択をする。監視・自動化・テストは、機能より先に効いてくる。
   「作れるか」ではなく「5年後も自分が保守できるか」で選ぶ。

---

## 4. マイルストーン

### M0 — 事故を止める（最優先・数日）

放置すると実害が出るもの。**これが終わるまで機能追加に着手しない。**

| | タスク | 理由 |
|---|---|---|
| 1 | prettier 整形 | CI が赤いままだと以降の変更で原因が切り分けられない |
| 2 | **1-1** ETL の件数安全装置 | 次に ETL を回したとき事故る。数行で塞げる |
| 3 | **1-3** Web版の通知 | Web版で**エラーが一切見えない**。既に起きている問題 |
| 4 | **1-2** アップロード上限 | ディスク枯渇でサービス全停止に繋がる |
| 5 | **1-4** 入力値の上限 | 数行 |
| 6 | **5-3** docker-compose 堅牢化 | 落ちる頻度に直結。特にログローテーションとメモリ上限 |

> **1-1 → 2-1 の順序は絶対。** 逆にすると全ポケふたが「撤去済み」になる事故が起きうる。

---

### M1 — 公開に耐える状態（v1.0）

他人に勧められる品質。ここまでで「完成」と言ってよい。

**データの正しさ**
- **2-1** 撤去ポケふたの論理削除 … 分母が汚れ続ける前に
- **2-3** EXIF 座標を DB に残さない … 預かるべきでないものを預からない
- **2-5** アカウント削除の順序

**壊れて見える箇所**
- **3-1** グリッド最終行 … **記録1件の新規ユーザーが必ず踏む**
- **3-2** レスポンシブ列数
- **3-3** タップ領域 44px
- **3-4** PC版のヘッダー/タブバー幅
- **3-6** 日付を JST 固定

**足りない基本操作**
- **2-2** 写真の個別削除・主写真変更
- **6-5** プルトゥリフレッシュの統一
- **6-2** 空状態を次の行動に繋げる
- **4-5** ゲスト同期のフィードバック

**初見のユーザーが迷わない**
- **6-1** オンボーディングと権限の文脈化 … 位置情報の許可率に直結

**運用**
- **5-2** パスワードリセット … ユーザーが増える前に。復帰手段がないのは実質データ損失

---

### M2 — 偶然を目的に変える（v1.1）

**ここが最も価値が高い。** ②気づき → ③目的化 の関門を越えさせる。

| | タスク | 担う段階 |
|---|---|---|
| 0 | **7-9** ゲストでも写真を撮って記録できる（下記・新規、フェーズ1） | **①偶然の発見。ここが欠けていた** |
| 1 | **7-4** 記録直後に「次」を見せる（下記・新規） | ②気づき |
| 2 | **7-5** 市区町村単位の目標（下記・新規） | **③目的化。最重要** |
| 3 | **6-3** 行きたいリスト | ③目的化の受け皿 |
| 4 | **6-6** 地図から直接記録 | 探索中のタップ数を減らす |
| 5 | **7-1** 振り返りの充実 | ④継続 |
| 6 | **4-4** アップロード UX（進捗・プレビュー・NONE のトーン） | 記録の完了率 |
| 7 | **6-7** 触覚フィードバック | 屋外で画面を見ていなくても伝わる |
| 8 | **4-3** 新着ポケふた | ④継続の理由 |

> **7-4 と 7-5 はセットで効く。** 7-4 だけだと「他にもあるんだ」で終わる。
> 7-5 の「あと1つでこの市はコンプリート」があって初めて、次の外出の目的になる。
>
> **6-3 の位置づけに注意。** 事前の旅行計画ツールではなく、
> **「行くと決めた場所を溜めておく」**ための機能。
> 入口は地図・詳細画面・7-5 のエリア画面であって、独立した計画画面ではない。

---

### M3 — 広がり（v2.0）

ユーザーが増えてから効いてくるもの。

- **4-1** 検索
- **4-2** ポケモン別図鑑 … 収集軸がもう1本増える
- **5-1** 静的生成 + JSON バンドル … SEO 流入の土台
- **6-9** シェア … 流入経路
- **7-2** 年間まとめ（下記・新規）
- **6-8** ダークモード
- **6-10** ナビ連携
- **2-4** インデックス整理

---

### M4 — 規模が要求したら

先回りしない。必要になってから。

- **5-4** Cloudflare R2 移行 … 規約リスクは実際の配信量が増えてから
- **4-6** 写真の転載チェック … **下記「やらないこと」を参照**
- ゲスト記録のバルク同期 … `syncGuestCollectionsToAccount`（テキストのみ）が
  記録ごとに1往復する性能課題。**当時はゲストが写真を持てない前提で書いた
  項目で、7-9（ゲストの写真記録）とは別物。** 7-9はフェーズ1（端末保存）の
  みで、同期そのもの（フェーズ2）はまだ実装しない。フェーズ2に着手する
  段階で、この性能課題を踏まえた設計にすること
- メール確認

---

## 5. 新規タスク

TASKS.md に無いが、発見した人を探す人に変えるために必要なもの。

### 7-4. 記録直後に「次」を見せる

**このロードマップで最も重要なタスク。**

**背景**
現在、記録が完了すると `CelebrationModal` が出て終わる。
しかし**ここがアプリで最も重要な瞬間**。旅先でたまたま1つ見つけた人が、
「他にもあるのか」と気づくかどうかが、ここで決まる。

今は気づく手段がない。モーダルを閉じると元の画面に戻るだけ。

**変更内容**

記録完了時のモーダルに、達成の演出**に続けて**次への導線を出す。

1. **この都道府県の残り数** … 「兵庫県にはあと57個あります」
   すでに `progress` で取得しているデータで表示できる。
2. **一番近い未収集ポケふた** … 「一番近いのは 3.2km 先の◯◯」＋サムネイル
   `/nearby` を収集済み除外で叩く。**タップで地図か詳細へ飛べること。**
3. **全国の規模を伝える** … 初回記録時のみ「全国に481箇所あります」
   1件目のユーザーは、そもそも規模を知らない。
4. **市区町村コンプリートが近い場合** … 「あと1つでこの市はコンプリート」
   到達可能な目標のほうが動機になる。都道府県は遠すぎる場合がある。

**設計上の注意**
- **祝福を邪魔しないこと。** まず達成を祝い、その**後**に次を提示する。
  同時に出すと、どちらの印象も薄まる。
- メダルが `NONE` のときは、演出のトーンを落としたうえで次への導線は残す
  （4-4 と整合させる）。
- 追加の API 往復を増やさない。4-4 で「アップロードのレスポンスに
  `collectedCount` と当該県の進捗を含める」と決めているので、
  **そこに最近傍の未収集ポケふたも含める**のが自然。

**受け入れ基準**
- 初めて記録したユーザーが、モーダルを閉じる前に「他にもある」ことを認識できる
- 次の候補にワンタップで到達できる
- API 往復が記録1回につき1往復に収まっている

---

### 7-5. 市区町村単位の目標をつくる（③目的化）

**7-4 と並んで、このロードマップの中核。**

**背景**
現在、進捗の粒度は都道府県（47件）しかない。しかしこの粒度は**目的化には粗すぎる**。

- 北海道58個、東京都30個台 … 「いつか集める」で終わる
- 12/57 のような数字は、次の行動に結びつかない

一方 `PokeLid.municipality` は既にデータとして存在するのに、**集計にも表示にも
使われていない**。市区町村なら1〜4個程度で、「あと1つ」が頻繁に発生する。

> 「兵庫県 12/57」より「**西宮市 3/4 — あと1つ**」のほうが人は動く。
> 今日の帰り道で寄れるからである。

**変更内容**

1. **市区町村単位の進捗を集計する**
   `progress` API に市区町村の粒度を足す。`municipality` は文字列なので、
   同名の市町村が複数県にある点に注意（府中市、伊達市など）。
   **`prefectureId` + `municipality` の組で一意に扱うこと。**

2. **「あと少し」を前面に出す**
   ホーム画面に、コンプリートに近い市区町村を数件出す。
   - 「あと1つ」を最優先で表示する
   - 現在地から近い順を優先する（位置情報がある場合）
   - **達成不可能に見える表示はしない。** 残り10個の市を出しても意味がない

3. **今日行ける範囲を示す**
   「現在地から10km以内に未収集が7個」のような、**移動可能な範囲**での提示。
   都道府県の枠を越えて集計すること（県境付近のユーザーに効く）。

4. **市区町村コンプリートを祝う**
   `CelebrationModal` のマイルストーンに市区町村を追加する。
   都道府県コンプリートより遥かに頻度が高く、**現実的に到達できる達成**になる。

5. **都道府県詳細を市区町村でグループ化する**
   58件のフラットな一覧より、市区町村セクションのほうが「あと1つの市」が見つけやすい。
   3-1 のグリッドと両立する形を検討すること。

**受け入れ基準**
- ホーム画面を開いたとき、**今日または今週行ける目標**が最低1つ見えている
- 同名の市区町村が別の都道府県にあっても混ざらない
- 市区町村コンプリート時に祝福が出る
- 追加の API 往復を増やさない（`progress` のレスポンスに含める）

**設計上の注意**
「あと1つ」が1件もないユーザーもいる。その場合は
「現在地から一番近い未収集」（7-4 と同じデータ）にフォールバックすること。
**目標が空の画面を見せない。**

---



### 7-1. 振り返りを厚くする（④継続）

**背景**
コレクション画面の統計は「訪問した都道府県 / 🥇獲得数 / 最初と最新の記録」のみ。
旅行から帰ってきた後にアプリを開く理由としては弱い。

**変更内容**
- **移動した総距離** … 収集したポケふた間の距離を積算。「日本一周分」など単位を添える
- **制覇した地方** … 都道府県より粗い粒度の達成感。地方コンプリートは現実的に到達可能
- **月別・年別の記録推移** … 簡単な棒グラフでよい
- **🥇率** … 「証明された訪問」の割合。メダル制度の芯を前に出す
- **最長連続記録月数** など、続ける動機になる指標

**注意**
指標を増やしすぎない。**5個程度に絞る。** 数字が多いと逆に何も印象に残らない。

---

### 7-2. 年間まとめ

**背景**
1年分の記録を振り返る体験は、シェアとの相性が非常に良い。
年末に一度だけ、強い再訪動機を作れる。

**変更内容**
- 「2026年のポケふたコレクト」として、その年の記録をまとめた1枚の画像を生成
- 訪れた都道府県、記録数、🥇数、移動距離、最も遠くまで行った記録
- 6-9 のシェア機能と統合する
- 年末（12月）にホーム画面から導線を出す

---

### 7-3. データエクスポート（対応済み・2026-08-13）

**背景**
個人開発のサービスに大切な写真を預けることへの不安は、当然ある。
**「いつでも自分のデータを取り出せる」ことは、機能ではなく信頼の問題。**
MONETIZATION.md §13.4（広告収益化の判断に伴うIPリスク整理）で、削除要請等に
よるサービス停止時にユーザーが記録・写真を取り出す手段がないことが判明し、
判断原則1（写真は二度と撮り直せない）に照らして最優先で着手した。

**実装内容**
- 設定画面の「データをダウンロードする」から、自分の全記録を ZIP で
  ダウンロードできる（`apps/api/src/routes/export.ts`）
  - 記録一覧（`collections.json` / `collections.csv`）— ポケふた名・
    都道府県・市区町村・訪問日・メダル・メモ・距離
  - アップロードした写真の原本（サムネイルではない、`Photo.filePath`）
  - `README.txt`
- **非同期ジョブは見送り、`archiver` によるストリーミング生成を採用した。**
  実運用の写真枚数は数十枚程度（リサイズ後1枚あたり数MB）で、ジョブ
  キュー・完了通知の仕組みを新設するほどの規模ではないと判断（個人開発・
  保守コスト最小、判断原則4）。メモリに全体をバッファせず `archive.file()`
  でディスクから直接ストリームするため、1-2の上限（500MB/ユーザー）まで
  達していてもメモリ使用量は一定に保たれる
- `POST /api/export`（要ログイン・レート制限 5回/日）が短命署名トークン
  （10分、`signPhotoAccessToken` と同じ HMAC 方式を流用）付きの
  ダウンロードURLを発行し、`GET /api/export/download` がそのトークンだけで
  認可する（`<Image>` と同じ理由：ネイティブの共有/保存UIに
  Authorizationヘッダーは乗せられない）
- レート制限は「実際に重い処理が走る回数」を絞るため生成の起点
  （`POST /api/export`）に置き、発行済みトークンの再利用（二重クリック等）
  はTTL内であれば追加のレート制限なしで許容している
- 所有者チェックは既存方針どおり404で存在を隠す（トークン自体が対象
  ユーザーを保証するため、追加のDB照合は不要）
- アカウント削除の確認ダイアログに「先にダウンロードを」という案内を追加

**優先度**
M1 に入れてもよい。実装は重くないわりに、ユーザーの安心感への寄与が大きい。

---

### 7-9. ゲストでも写真を撮って記録できるようにする（①偶然の発見）

**背景**

現在、ゲスト（未ログイン）は「訪問した」というテキストの記録しか残せない。
写真を残せないため、メダル（🥇🥈）を一切体験できない。**このアプリの
存在理由と定義したメダル制度が、ログインするまで体験できない構造になって
いる。** 偶然ポケふたを見つけて撮った1枚こそ、①偶然の発見の入口になるべき
瞬間なのに、今はそこでログインを要求して離脱させている。

**フェーズ分割**

- **フェーズ1（今回）**: 端末保存まで。撮影・EXIFからの距離計算・端末への
  永続保存・プレビュー表示。サーバーへの送信はしない
- **フェーズ2（別タスク）**: ログイン時にサーバーへまとめてアップロードする
  同期。上記「意図的にやらないこと」欄の「ゲスト記録のバルク同期」の性能
  課題（レート制限・部分失敗時の扱い）を踏まえて設計すること

**フェーズ1の変更内容**

1. 撮影・ライブラリ選択・プレビューの導線（`onPickPhoto`/`PhotoPreviewModal`）
   は元々ゲスト/ログイン中立なので、詳細画面の`{user ? 撮影ボタン :
   ログイン誘導}`という分岐を外し、ゲストにも撮影・プレビューまで通す
2. 確定時、ゲストならサーバーではなく端末（Web: IndexedDB、Native:
   `expo-file-system`）に保存する。EXIFはリサイズ前に読むこと（リサイズは
   位置情報を含むメタデータを削るため）
3. **メダルは確定表示しない。** サーバーが最終権威という構造を守るため、
   ゲスト状態では`haversineDistanceMeters`で計算した距離のみ見せる
   （「金メダルだったのに同期後に銀になった」を避ける）
4. 「保存されました」と言い切らない。Safari は7日間そのサイトへの操作が
   ないとスクリプトが作ったデータを自動削除するため（OPFSでも同じ）、
   「ログインすると安全に保管される」という前向きな言い方に留める

**受け入れ基準**
- ゲストが写真を撮り、詳細画面とコレクション画面に表示される
- 距離が表示され、メダルは確定表示されない
- ブラウザを閉じて開き直しても写真が残っている（Safariの7日eviction条件を
  除く）
- ネイティブでも同じ導線が動く

---

## 6. 意図的にやらないこと

**やらない判断を明文化しておくことは、やることを決めるのと同じくらい重要。**

### ランキング・リーダーボード

収集数を競わせると、不正のインセンティブが跳ね上がる。
現在の写真転載チェック（4-6）は**ランキングを導入しない限り過剰**。
順位という報酬があって初めて、不正対策のコストが正当化される。

→ **4-6 はランキングを入れると決めたときに実装する。** それまでは M4 に置いておく。
既存のメダル制度が既に「証明された訪問」を担保しているので、当面はそれで足りる。

### 位置情報の常時取得（ジオフェンス通知）

「近くに未収集のポケふたがあります」という通知は魅力的に見えるが、
バッテリー消費と、常時位置情報を許可させることのプライバシーコストが釣り合わない。
このアプリが預かる情報を最小に保つ方針（2-3 と同じ考え方）とも矛盾する。

### SNS 機能（フォロー・コメント・いいね）

モデレーションの負荷が個人開発の許容量を超える。
シェアは外部 SNS に委ねる（6-9）。

### ネイティブアプリのストア配信

現状 Web で完結しており、PWA でホーム画面に追加できる。
ストア審査・両 OS のビルド・アップデート配信の維持コストは、
ユーザー数が明確に要求するまで負わない。

---

## 7. 「完成」の定義

以下を全部満たしたら v1.0 として公開してよい。

- [ ] CI が緑（typecheck / lint / prettier / test）
- [ ] 新規ユーザーが、説明なしに最初の1件を記録できる
- [ ] **最初の1件を記録したユーザーが、他にもポケふたがあることに気づける**
- [ ] **ホーム画面に、今日または今週行ける目標が1つ以上見えている**
- [ ] 記録を削除・修正できる
- [ ] パスワードを忘れても復帰できる
- [x] 自分のデータを取り出せる（7-3、2026-08-13対応）
- [ ] 圏外・サーバー障害時に、何が起きたか画面から分かる
- [ ] 320px 幅・フォント最大設定で、どの画面も崩れない
- [ ] バックアップが自動で回り、**復元を一度は実際に試した**
- [ ] 外形監視が動いている

最後の2つは忘れられがちだが、**復元を試していないバックアップは無いのと同じ**。

---

## 8. この判断について

ここに書いた優先順位は私の判断であって、正解ではない。

**確定している方針**（設計の起点。ここは動かさない）

> **偶然を、目的に変える。**
> 最初の1つはたまたま見つけたものでいい。それは導入にすぎない。
> 2つ目からは「探しに行く」ものにする。

機能を足すか迷ったら「**次の外出の目的になるか**」で判断する。
7-4（気づき）と 7-5（目的化）を最優先に置いているのはこの理由による。

**自信が低い点**

1. **（検証済み）目標の粒度を市区町村にしたこと** … 7-5 実装時に実データ
   （481件・399市区町村）を集計した。想定（1〜4個程度）とは異なり、9割の
   市区町村が1個しか持たず、「あと1つ」が意味を持つのは35市区町村（8.8%）
   のみだった（詳細は §2「③目的化がいちばん難しい」）。市区町村単位の目標は
   `total >= 2` に絞って残したが、③目的化の主役は市区町村の粒度に依存しない
   「今日行ける範囲に未収集N個」に置き換えた。区単位への変更は不要と判断
   した — 横浜市・京都市など件数が多い都市（5個クラス）でも実際は同じ区内に
   密集しており（例: 横浜市の5個は中区・西区のみ）、市区町村より細かい粒度に
   する実益が薄いため。
2. **「ランキングをやらない」** … 収集アプリとしては競争要素が効く可能性もある。
   ただし入れるなら不正対策とセットで、後戻りしにくい判断になる。
3. **「ストア配信をしない」** … 日本のユーザー層を考えるとネイティブアプリへの期待は
   高いかもしれない。
4. **（決定・2026-08-13）広告収益化を進めること** … MONETIZATION.md §13
   「ポケモンIPとの関係」で整理した論点（ポケモン公式サイトの利用規約が
   複製・再配信・営利目的での利用を明文で禁止していること、包括的な
   ファンコンテンツガイドラインは存在しないこと、削除要請を受ければ一覧・
   地図・図鑑という中核機能が失われ実質的なサービス停止になりうること）
   を踏まえたうえで、そのリスクを引き受けて進める判断をした。法的に白黒
   はっきりしているわけではなく、削除要請や規約違反の指摘を受ける可能性は
   消えていない — グレーな判断であることを認めた上での決定であり、この
   ロードマップの中でも特に自信が低い一件。
   進める前提として、リスクが実際に顕在化したときの被害を先に小さくして
   おいた: ユーザーが自分の記録・写真を取り出す手段が無かった問題（7-3
   データエクスポート、§5参照）と、ETLの User-Agent が
   `family use, non-commercial` と事実と異なる申告をしていた問題
   （`apps/api/etl/scrape.ts`）。広告SDK自体の技術設計・フェーズ分け・
   ロールバック基準は MONETIZATION.md 本体（§0〜§12）に委ねる。

**実際に旅先で何回か使ってみることが、この文書を読み返すより有益。**
机上では出てこない問題（圏外での挙動、片手操作、直射日光下の視認性）が必ず見つかる。
そこで得た違和感のほうが、ここに書いた優先順位より信頼できる。
